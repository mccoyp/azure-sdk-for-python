# pylint: disable=line-too-long,useless-suppression,too-many-lines
# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
# Code generated by Microsoft (R) Python Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is regenerated.
# --------------------------------------------------------------------------
from collections.abc import MutableMapping
from io import IOBase
import json
from typing import Any, Callable, IO, Iterator, Optional, TypeVar, Union, cast, overload

from azure.core import PipelineClient
from azure.core.exceptions import (
    ClientAuthenticationError,
    HttpResponseError,
    ResourceExistsError,
    ResourceNotFoundError,
    ResourceNotModifiedError,
    StreamClosedError,
    StreamConsumedError,
    map_error,
)
from azure.core.pipeline import PipelineResponse
from azure.core.polling import LROPoller, NoPolling, PollingMethod
from azure.core.polling.base_polling import LROBasePolling
from azure.core.rest import HttpRequest, HttpResponse
from azure.core.tracing.decorator import distributed_trace
from azure.core.utils import case_insensitive_dict

from .. import models as _models
from .._configuration import MapsRouteClientConfiguration
from .._utils.model_base import SdkJSONEncoder, _deserialize, _failsafe_deserialize
from .._utils.serialization import Deserializer, Serializer

JSON = MutableMapping[str, Any]
T = TypeVar("T")
ClsType = Optional[Callable[[PipelineResponse[HttpRequest, HttpResponse], T, dict[str, Any]], Any]]

_SERIALIZER = Serializer()
_SERIALIZER.client_side_validation = False


def build_route_post_route_directions_request(  # pylint: disable=name-too-long
    *, client_id: Optional[str] = None, accept_language: Optional[str] = None, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", None)

    # Construct URL
    _url = "/route/directions"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    if accept_language is not None:
        _headers["Accept-Language"] = _SERIALIZER.header("accept_language", accept_language, "str")
    if accept is not None:
        _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_post_route_directions_batch_request(  # pylint: disable=name-too-long
    *, client_id: Optional[str] = None, accept_language: Optional[str] = None, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", "application/json")

    # Construct URL
    _url = "/route/directions:batch"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if accept_language is not None:
        _headers["Accept-Language"] = _SERIALIZER.header("accept_language", accept_language, "str")
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_post_snap_to_roads_request(
    *, client_id: Optional[str] = None, accept_language: Optional[str] = None, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", None)

    # Construct URL
    _url = "/route/snapToRoads"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    if accept_language is not None:
        _headers["Accept-Language"] = _SERIALIZER.header("accept_language", accept_language, "str")
    if accept is not None:
        _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_post_snap_to_roads_batch_request(  # pylint: disable=name-too-long
    *, client_id: Optional[str] = None, accept_language: Optional[str] = None, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", "application/json")

    # Construct URL
    _url = "/route/snapToRoads:batch"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    if accept_language is not None:
        _headers["Accept-Language"] = _SERIALIZER.header("accept_language", accept_language, "str")
    _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_post_route_range_request(*, client_id: Optional[str] = None, **kwargs: Any) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", None)

    # Construct URL
    _url = "/route/range"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    if accept is not None:
        _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_post_route_range_batch_request(  # pylint: disable=name-too-long
    *, client_id: Optional[str] = None, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", "application/json")

    # Construct URL
    _url = "/route/range:batch"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_post_route_matrix_request(*, client_id: Optional[str] = None, **kwargs: Any) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", "application/geo+json")

    # Construct URL
    _url = "/route/matrix"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_get_route_operations_status_request(  # pylint: disable=name-too-long
    id: str, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", "application/json")

    # Construct URL
    _url = "/route/operations/{id}"
    path_format_arguments = {
        "id": _SERIALIZER.url("id", id, "str"),
    }

    _url: str = _url.format(**path_format_arguments)  # type: ignore

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="GET", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_get_route_operations_result_request(  # pylint: disable=name-too-long
    id: str, *, client_id: Optional[str] = None, accept_language: Optional[str] = None, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    accept = _headers.pop("Accept", "application/json")

    # Construct URL
    _url = "/route/operations/{id}/result"
    path_format_arguments = {
        "id": _SERIALIZER.url("id", id, "str"),
    }

    _url: str = _url.format(**path_format_arguments)  # type: ignore

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")
    if accept_language is not None:
        _headers["Accept-Language"] = _SERIALIZER.header("accept_language", accept_language, "str")
    _headers["Accept"] = _SERIALIZER.header("accept", accept, "str")

    return HttpRequest(method="GET", url=_url, params=_params, headers=_headers, **kwargs)


def build_route_post_route_matrix_async_request(  # pylint: disable=name-too-long
    *, client_id: Optional[str] = None, **kwargs: Any
) -> HttpRequest:
    _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
    _params = case_insensitive_dict(kwargs.pop("params", {}) or {})

    content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
    api_version: str = kwargs.pop("api_version", _params.pop("api-version", "2025-01-01"))
    # Construct URL
    _url = "/route/matrix:async"

    # Construct parameters
    _params["api-version"] = _SERIALIZER.query("api_version", api_version, "str")

    # Construct headers
    if content_type is not None:
        _headers["Content-Type"] = _SERIALIZER.header("content_type", content_type, "str")
    if client_id is not None:
        _headers["x-ms-client-id"] = _SERIALIZER.header("client_id", client_id, "str")

    return HttpRequest(method="POST", url=_url, params=_params, headers=_headers, **kwargs)


class RouteOperations:
    """
    .. warning::
        **DO NOT** instantiate this class directly.

        Instead, you should access the following operations through
        :class:`~azure.maps.route.MapsRouteClient`'s
        :attr:`route` attribute.
    """

    def __init__(self, *args, **kwargs) -> None:
        input_args = list(args)
        self._client: PipelineClient = input_args.pop(0) if input_args else kwargs.pop("client")
        self._config: MapsRouteClientConfiguration = input_args.pop(0) if input_args else kwargs.pop("config")
        self._serialize: Serializer = input_args.pop(0) if input_args else kwargs.pop("serializer")
        self._deserialize: Deserializer = input_args.pop(0) if input_args else kwargs.pop("deserializer")

    @overload
    def post_route_directions(
        self,
        route_directions_request: _models.DirectionsRequest,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.RouteDirectionsResponse:
        """Use to get the best route between an origin and destination for automobile, commercial trucks
        and walking routes.

        The ``Route Directions`` API is an HTTP ``POST`` request that returns the ideal
        route between an origin and destination for automobile (driving), commercial
        trucks and walking routes. The route passes through a series of waypoints if
        specified. A waypoint is a geographical location defined by longitude and
        latitude that is used for navigational purposes. The route considers factors
        such as current traffic and the typical road speeds on the requested day of the
        week and time of day.

        The API returns the distance, estimated travel time, and a representation of
        the route geometry. More routing information such as an optimized waypoint
        order or turn by turn instructions is also available, depending on the
        parameters used.

        The Route Directions considers local laws, vehicle dimensions, cargo type, max
        speed, bridge and tunnel heights to calculate the truck specific routes and
        avoid complex maneuvers and difficult roads. Not all trucks can travel the same
        routes as other vehicles due to certain restrictions based on the vehicle
        profile or cargo type. For example, highways often have separate speed limits
        for trucks, some roads don't allow trucks with flammable or hazardous
        materials, and there can be height and weight restriction on bridges.

        Up to 150 waypoints and 10 viaWaypoints between any two waypoints is supported.
        Each set of waypoints creates a separate route Leg. ViaWaypoints define the
        route path and can be used for route creation through specific locations, but
        they don't create route Legs.

        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_directions_request: Request body of RouteDirections API in GeoJSON format.
         Required.
        :type route_directions_request: ~azure.maps.route.models.DirectionsRequest
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: RouteDirectionsResponse. The RouteDirectionsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteDirectionsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_directions(
        self,
        route_directions_request: JSON,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.RouteDirectionsResponse:
        """Use to get the best route between an origin and destination for automobile, commercial trucks
        and walking routes.

        The ``Route Directions`` API is an HTTP ``POST`` request that returns the ideal
        route between an origin and destination for automobile (driving), commercial
        trucks and walking routes. The route passes through a series of waypoints if
        specified. A waypoint is a geographical location defined by longitude and
        latitude that is used for navigational purposes. The route considers factors
        such as current traffic and the typical road speeds on the requested day of the
        week and time of day.

        The API returns the distance, estimated travel time, and a representation of
        the route geometry. More routing information such as an optimized waypoint
        order or turn by turn instructions is also available, depending on the
        parameters used.

        The Route Directions considers local laws, vehicle dimensions, cargo type, max
        speed, bridge and tunnel heights to calculate the truck specific routes and
        avoid complex maneuvers and difficult roads. Not all trucks can travel the same
        routes as other vehicles due to certain restrictions based on the vehicle
        profile or cargo type. For example, highways often have separate speed limits
        for trucks, some roads don't allow trucks with flammable or hazardous
        materials, and there can be height and weight restriction on bridges.

        Up to 150 waypoints and 10 viaWaypoints between any two waypoints is supported.
        Each set of waypoints creates a separate route Leg. ViaWaypoints define the
        route path and can be used for route creation through specific locations, but
        they don't create route Legs.

        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_directions_request: Request body of RouteDirections API in GeoJSON format.
         Required.
        :type route_directions_request: JSON
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: RouteDirectionsResponse. The RouteDirectionsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteDirectionsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_directions(
        self,
        route_directions_request: IO[bytes],
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.RouteDirectionsResponse:
        """Use to get the best route between an origin and destination for automobile, commercial trucks
        and walking routes.

        The ``Route Directions`` API is an HTTP ``POST`` request that returns the ideal
        route between an origin and destination for automobile (driving), commercial
        trucks and walking routes. The route passes through a series of waypoints if
        specified. A waypoint is a geographical location defined by longitude and
        latitude that is used for navigational purposes. The route considers factors
        such as current traffic and the typical road speeds on the requested day of the
        week and time of day.

        The API returns the distance, estimated travel time, and a representation of
        the route geometry. More routing information such as an optimized waypoint
        order or turn by turn instructions is also available, depending on the
        parameters used.

        The Route Directions considers local laws, vehicle dimensions, cargo type, max
        speed, bridge and tunnel heights to calculate the truck specific routes and
        avoid complex maneuvers and difficult roads. Not all trucks can travel the same
        routes as other vehicles due to certain restrictions based on the vehicle
        profile or cargo type. For example, highways often have separate speed limits
        for trucks, some roads don't allow trucks with flammable or hazardous
        materials, and there can be height and weight restriction on bridges.

        Up to 150 waypoints and 10 viaWaypoints between any two waypoints is supported.
        Each set of waypoints creates a separate route Leg. ViaWaypoints define the
        route path and can be used for route creation through specific locations, but
        they don't create route Legs.

        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_directions_request: Request body of RouteDirections API in GeoJSON format.
         Required.
        :type route_directions_request: IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: RouteDirectionsResponse. The RouteDirectionsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteDirectionsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def post_route_directions(
        self,
        route_directions_request: Union[_models.DirectionsRequest, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.RouteDirectionsResponse:
        """Use to get the best route between an origin and destination for automobile, commercial trucks
        and walking routes.

        The ``Route Directions`` API is an HTTP ``POST`` request that returns the ideal
        route between an origin and destination for automobile (driving), commercial
        trucks and walking routes. The route passes through a series of waypoints if
        specified. A waypoint is a geographical location defined by longitude and
        latitude that is used for navigational purposes. The route considers factors
        such as current traffic and the typical road speeds on the requested day of the
        week and time of day.

        The API returns the distance, estimated travel time, and a representation of
        the route geometry. More routing information such as an optimized waypoint
        order or turn by turn instructions is also available, depending on the
        parameters used.

        The Route Directions considers local laws, vehicle dimensions, cargo type, max
        speed, bridge and tunnel heights to calculate the truck specific routes and
        avoid complex maneuvers and difficult roads. Not all trucks can travel the same
        routes as other vehicles due to certain restrictions based on the vehicle
        profile or cargo type. For example, highways often have separate speed limits
        for trucks, some roads don't allow trucks with flammable or hazardous
        materials, and there can be height and weight restriction on bridges.

        Up to 150 waypoints and 10 viaWaypoints between any two waypoints is supported.
        Each set of waypoints creates a separate route Leg. ViaWaypoints define the
        route path and can be used for route creation through specific locations, but
        they don't create route Legs.

        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_directions_request: Request body of RouteDirections API in GeoJSON format. Is one
         of the following types: DirectionsRequest, JSON, IO[bytes] Required.
        :type route_directions_request: ~azure.maps.route.models.DirectionsRequest or JSON or IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: RouteDirectionsResponse. The RouteDirectionsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteDirectionsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.RouteDirectionsResponse] = kwargs.pop("cls", None)

        content_type = content_type or "application/geo+json"
        _content = None
        if isinstance(route_directions_request, (IOBase, bytes)):
            _content = route_directions_request
        else:
            _content = json.dumps(route_directions_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_route_directions_request(
            client_id=client_id,
            accept_language=accept_language,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        response_headers = {}
        response_headers["Content-Type"] = self._deserialize("str", response.headers.get("Content-Type"))

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.RouteDirectionsResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, response_headers)  # type: ignore

        return deserialized  # type: ignore

    @overload
    def post_route_directions_batch(
        self,
        route_directions_batch_request: _models.DirectionsBatchRequestBody,
        *,
        client_id: Optional[str] = None,
        accept_language: Optional[str] = None,
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.DirectionsBatchResponse:
        """Use to send a batch of queries to the `Route Directions
        </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_ API in a single
        synchronous request.

        The ``Route Directions Batch`` API is an HTTP ``POST`` request that sends batches
        of up to **100** queries in a single call to the `Route
        Directions </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_
        API.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/directions:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *directions* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *directions* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "fastestWithoutTraffic",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 3,
                 "travelMode": "driving"
               },
               {
                 "optionalId": "a191de3c-1268-4986-98f0-03f0a5d9302a",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "shortest",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 2,
                 "travelMode": "driving"
               }
             ]
           }

        A *directions* batchItem object can accept any of the supported *directions*
        `Request
        body </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`DirectionsResponse`](/rest/api/maps/route/post-directions#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_directions_batch_request: The list of route directions queries/requests to
         process. The list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Required.
        :type route_directions_batch_request: ~azure.maps.route.models.DirectionsBatchRequestBody
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: DirectionsBatchResponse. The DirectionsBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.DirectionsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_directions_batch(
        self,
        route_directions_batch_request: JSON,
        *,
        client_id: Optional[str] = None,
        accept_language: Optional[str] = None,
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.DirectionsBatchResponse:
        """Use to send a batch of queries to the `Route Directions
        </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_ API in a single
        synchronous request.

        The ``Route Directions Batch`` API is an HTTP ``POST`` request that sends batches
        of up to **100** queries in a single call to the `Route
        Directions </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_
        API.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/directions:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *directions* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *directions* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "fastestWithoutTraffic",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 3,
                 "travelMode": "driving"
               },
               {
                 "optionalId": "a191de3c-1268-4986-98f0-03f0a5d9302a",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "shortest",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 2,
                 "travelMode": "driving"
               }
             ]
           }

        A *directions* batchItem object can accept any of the supported *directions*
        `Request
        body </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`DirectionsResponse`](/rest/api/maps/route/post-directions#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_directions_batch_request: The list of route directions queries/requests to
         process. The list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Required.
        :type route_directions_batch_request: JSON
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: DirectionsBatchResponse. The DirectionsBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.DirectionsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_directions_batch(
        self,
        route_directions_batch_request: IO[bytes],
        *,
        client_id: Optional[str] = None,
        accept_language: Optional[str] = None,
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.DirectionsBatchResponse:
        """Use to send a batch of queries to the `Route Directions
        </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_ API in a single
        synchronous request.

        The ``Route Directions Batch`` API is an HTTP ``POST`` request that sends batches
        of up to **100** queries in a single call to the `Route
        Directions </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_
        API.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/directions:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *directions* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *directions* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "fastestWithoutTraffic",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 3,
                 "travelMode": "driving"
               },
               {
                 "optionalId": "a191de3c-1268-4986-98f0-03f0a5d9302a",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "shortest",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 2,
                 "travelMode": "driving"
               }
             ]
           }

        A *directions* batchItem object can accept any of the supported *directions*
        `Request
        body </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`DirectionsResponse`](/rest/api/maps/route/post-directions#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_directions_batch_request: The list of route directions queries/requests to
         process. The list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Required.
        :type route_directions_batch_request: IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: DirectionsBatchResponse. The DirectionsBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.DirectionsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def post_route_directions_batch(
        self,
        route_directions_batch_request: Union[_models.DirectionsBatchRequestBody, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.DirectionsBatchResponse:
        """Use to send a batch of queries to the `Route Directions
        </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_ API in a single
        synchronous request.

        The ``Route Directions Batch`` API is an HTTP ``POST`` request that sends batches
        of up to **100** queries in a single call to the `Route
        Directions </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01>`_
        API.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/directions:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *directions* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *directions* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "fastestWithoutTraffic",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 3,
                 "travelMode": "driving"
               },
               {
                 "optionalId": "a191de3c-1268-4986-98f0-03f0a5d9302a",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.3368,
                         47.614988
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 0,
                       "pointType": "waypoint"
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "coordinates": [
                         -122.316067,
                         47.606356
                       ],
                       "type": "Point"
                     },
                     "properties": {
                       "pointIndex": 1,
                       "pointType": "waypoint"
                     }
                   }
                 ],
                 "optimizeRoute": "shortest",
                 "routeOutputOptions": [
                   "routeSummary"
                 ],
                 "maxRouteCount": 2,
                 "travelMode": "driving"
               }
             ]
           }

        A *directions* batchItem object can accept any of the supported *directions*
        `Request
        body </rest/api/maps/route/post-route-directions?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`DirectionsResponse`](/rest/api/maps/route/post-directions#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_directions_batch_request: The list of route directions queries/requests to
         process. The list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Is one of the
         following types: DirectionsBatchRequestBody, JSON, IO[bytes] Required.
        :type route_directions_batch_request: ~azure.maps.route.models.DirectionsBatchRequestBody or
         JSON or IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: DirectionsBatchResponse. The DirectionsBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.DirectionsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.DirectionsBatchResponse] = kwargs.pop("cls", None)

        content_type = content_type or "application/json"
        _content = None
        if isinstance(route_directions_batch_request, (IOBase, bytes)):
            _content = route_directions_batch_request
        else:
            _content = json.dumps(route_directions_batch_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_route_directions_batch_request(
            client_id=client_id,
            accept_language=accept_language,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.DirectionsBatchResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @overload
    def post_snap_to_roads(
        self,
        snap_to_roads_request: _models.SnapToRoadsRequest,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsResponse:
        """The Snap to Roads API accepts GPS point data, represented as longitude and
        latitude coordinates, and generates points that aligns with existing roadways
        on a map. This process, known as "snapping to roads", produces a series of
        objects that trace a path closely following the road network. The resulting
        data includes road names and their respective speed limits, pertinent to the
        traversed segments.

        Moreover, the Snap to Roads API offers an interpolation feature, which refines
        the GPS points to create a smoother route that adheres to the road's geometry.
        This functionality is especially beneficial for asset tracking and enhancing
        data visualization in mapping applications.

        [!Important]
        The GPS points must be within 6 kilometer of each other.





        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param snap_to_roads_request: Request body of SnapToRoads API in GeoJSON format. Required.
        :type snap_to_roads_request: ~azure.maps.route.models.SnapToRoadsRequest
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsResponse. The SnapToRoadsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_snap_to_roads(
        self,
        snap_to_roads_request: JSON,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsResponse:
        """The Snap to Roads API accepts GPS point data, represented as longitude and
        latitude coordinates, and generates points that aligns with existing roadways
        on a map. This process, known as "snapping to roads", produces a series of
        objects that trace a path closely following the road network. The resulting
        data includes road names and their respective speed limits, pertinent to the
        traversed segments.

        Moreover, the Snap to Roads API offers an interpolation feature, which refines
        the GPS points to create a smoother route that adheres to the road's geometry.
        This functionality is especially beneficial for asset tracking and enhancing
        data visualization in mapping applications.

        [!Important]
        The GPS points must be within 6 kilometer of each other.





        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param snap_to_roads_request: Request body of SnapToRoads API in GeoJSON format. Required.
        :type snap_to_roads_request: JSON
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsResponse. The SnapToRoadsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_snap_to_roads(
        self,
        snap_to_roads_request: IO[bytes],
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsResponse:
        """The Snap to Roads API accepts GPS point data, represented as longitude and
        latitude coordinates, and generates points that aligns with existing roadways
        on a map. This process, known as "snapping to roads", produces a series of
        objects that trace a path closely following the road network. The resulting
        data includes road names and their respective speed limits, pertinent to the
        traversed segments.

        Moreover, the Snap to Roads API offers an interpolation feature, which refines
        the GPS points to create a smoother route that adheres to the road's geometry.
        This functionality is especially beneficial for asset tracking and enhancing
        data visualization in mapping applications.

        [!Important]
        The GPS points must be within 6 kilometer of each other.





        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param snap_to_roads_request: Request body of SnapToRoads API in GeoJSON format. Required.
        :type snap_to_roads_request: IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsResponse. The SnapToRoadsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def post_snap_to_roads(
        self,
        snap_to_roads_request: Union[_models.SnapToRoadsRequest, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsResponse:
        """The Snap to Roads API accepts GPS point data, represented as longitude and
        latitude coordinates, and generates points that aligns with existing roadways
        on a map. This process, known as "snapping to roads", produces a series of
        objects that trace a path closely following the road network. The resulting
        data includes road names and their respective speed limits, pertinent to the
        traversed segments.

        Moreover, the Snap to Roads API offers an interpolation feature, which refines
        the GPS points to create a smoother route that adheres to the road's geometry.
        This functionality is especially beneficial for asset tracking and enhancing
        data visualization in mapping applications.

        [!Important]
        The GPS points must be within 6 kilometer of each other.





        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param snap_to_roads_request: Request body of SnapToRoads API in GeoJSON format. Is one of the
         following types: SnapToRoadsRequest, JSON, IO[bytes] Required.
        :type snap_to_roads_request: ~azure.maps.route.models.SnapToRoadsRequest or JSON or IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsResponse. The SnapToRoadsResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.SnapToRoadsResponse] = kwargs.pop("cls", None)

        content_type = content_type or "application/geo+json"
        _content = None
        if isinstance(snap_to_roads_request, (IOBase, bytes)):
            _content = snap_to_roads_request
        else:
            _content = json.dumps(snap_to_roads_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_snap_to_roads_request(
            client_id=client_id,
            accept_language=accept_language,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        response_headers = {}
        response_headers["Content-Type"] = self._deserialize("str", response.headers.get("Content-Type"))

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.SnapToRoadsResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, response_headers)  # type: ignore

        return deserialized  # type: ignore

    @overload
    def post_snap_to_roads_batch(
        self,
        snap_to_roads_batch_request: _models.SnapToRoadsBatchRequestBody,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsBatchResponse:
        """The Snap To Roads Batch API sends batches of up to **100** queries as a single
        call to the `Snap To Roads
        API </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01>`_. This
        API enables efficient processing of multiple queries in one request, reducing
        the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/snapToRoads:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *snap to roads* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 1 *snap to roads* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336691,
                         47.59085
                       ]
                     },
                     "properties": {
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336023,
                         47.601616
                       ]
                     },
                     "properties": {
                     }
                   }
                 ],
                 "includeSpeedLimit": true,
                 "travelMode": "driving"
               }
             ]
           }

        A *snap to roads* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`SnapToRoadsResponse`](/rest/api/maps/route/post-snap-to-roads#response) -
        If the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param snap_to_roads_batch_request: The list of Snap To Roads queries/requests to process. The
         list can contain a
         max of 100 queries for sync version and must contain at least 1 query. Required.
        :type snap_to_roads_batch_request: ~azure.maps.route.models.SnapToRoadsBatchRequestBody
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsBatchResponse. The SnapToRoadsBatchResponse is compatible with
         MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_snap_to_roads_batch(
        self,
        snap_to_roads_batch_request: JSON,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsBatchResponse:
        """The Snap To Roads Batch API sends batches of up to **100** queries as a single
        call to the `Snap To Roads
        API </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01>`_. This
        API enables efficient processing of multiple queries in one request, reducing
        the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/snapToRoads:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *snap to roads* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 1 *snap to roads* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336691,
                         47.59085
                       ]
                     },
                     "properties": {
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336023,
                         47.601616
                       ]
                     },
                     "properties": {
                     }
                   }
                 ],
                 "includeSpeedLimit": true,
                 "travelMode": "driving"
               }
             ]
           }

        A *snap to roads* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`SnapToRoadsResponse`](/rest/api/maps/route/post-snap-to-roads#response) -
        If the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param snap_to_roads_batch_request: The list of Snap To Roads queries/requests to process. The
         list can contain a
         max of 100 queries for sync version and must contain at least 1 query. Required.
        :type snap_to_roads_batch_request: JSON
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsBatchResponse. The SnapToRoadsBatchResponse is compatible with
         MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_snap_to_roads_batch(
        self,
        snap_to_roads_batch_request: IO[bytes],
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsBatchResponse:
        """The Snap To Roads Batch API sends batches of up to **100** queries as a single
        call to the `Snap To Roads
        API </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01>`_. This
        API enables efficient processing of multiple queries in one request, reducing
        the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/snapToRoads:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *snap to roads* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 1 *snap to roads* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336691,
                         47.59085
                       ]
                     },
                     "properties": {
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336023,
                         47.601616
                       ]
                     },
                     "properties": {
                     }
                   }
                 ],
                 "includeSpeedLimit": true,
                 "travelMode": "driving"
               }
             ]
           }

        A *snap to roads* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`SnapToRoadsResponse`](/rest/api/maps/route/post-snap-to-roads#response) -
        If the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param snap_to_roads_batch_request: The list of Snap To Roads queries/requests to process. The
         list can contain a
         max of 100 queries for sync version and must contain at least 1 query. Required.
        :type snap_to_roads_batch_request: IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsBatchResponse. The SnapToRoadsBatchResponse is compatible with
         MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def post_snap_to_roads_batch(
        self,
        snap_to_roads_batch_request: Union[_models.SnapToRoadsBatchRequestBody, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        accept_language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SnapToRoadsBatchResponse:
        """The Snap To Roads Batch API sends batches of up to **100** queries as a single
        call to the `Snap To Roads
        API </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01>`_. This
        API enables efficient processing of multiple queries in one request, reducing
        the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/snapToRoads:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *snap to roads* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 1 *snap to roads* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "FeatureCollection",
                 "features": [
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336691,
                         47.59085
                       ]
                     },
                     "properties": {
                     }
                   },
                   {
                     "type": "Feature",
                     "geometry": {
                       "type": "Point",
                       "coordinates": [
                         -122.336023,
                         47.601616
                       ]
                     },
                     "properties": {
                     }
                   }
                 ],
                 "includeSpeedLimit": true,
                 "travelMode": "driving"
               }
             ]
           }

        A *snap to roads* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`SnapToRoadsResponse`](/rest/api/maps/route/post-snap-to-roads#response) -
        If the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param snap_to_roads_batch_request: The list of Snap To Roads queries/requests to process. The
         list can contain a
         max of 100 queries for sync version and must contain at least 1 query. Is one of the following
         types: SnapToRoadsBatchRequestBody, JSON, IO[bytes] Required.
        :type snap_to_roads_batch_request: ~azure.maps.route.models.SnapToRoadsBatchRequestBody or JSON
         or IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: SnapToRoadsBatchResponse. The SnapToRoadsBatchResponse is compatible with
         MutableMapping
        :rtype: ~azure.maps.route.models.SnapToRoadsBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.SnapToRoadsBatchResponse] = kwargs.pop("cls", None)

        content_type = content_type or "application/geo+json"
        _content = None
        if isinstance(snap_to_roads_batch_request, (IOBase, bytes)):
            _content = snap_to_roads_batch_request
        else:
            _content = json.dumps(snap_to_roads_batch_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_snap_to_roads_batch_request(
            client_id=client_id,
            accept_language=accept_language,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.SnapToRoadsBatchResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @overload
    def post_route_range(
        self,
        route_range_request: _models.RouteRangeRequest,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        **kwargs: Any
    ) -> _models.RouteRangeResponse:
        """The Route Range API creates a polygon that depicts the area reachable from a
        given location within a certain threshold based on the specified time or
        distance budget. A polygon boundary (or Isochrone) is returned in a
        counterclockwise orientation as well as the precise polygon center which was
        the result of the origin point.

        The returned polygon can be used for spatial filtering to search for features
        of interest within the provided Isochrone.



        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_range_request: Request body of RouteRange API in GeoJSON format. Required.
        :type route_range_request: ~azure.maps.route.models.RouteRangeRequest
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :return: RouteRangeResponse. The RouteRangeResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_range(
        self,
        route_range_request: JSON,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        **kwargs: Any
    ) -> _models.RouteRangeResponse:
        """The Route Range API creates a polygon that depicts the area reachable from a
        given location within a certain threshold based on the specified time or
        distance budget. A polygon boundary (or Isochrone) is returned in a
        counterclockwise orientation as well as the precise polygon center which was
        the result of the origin point.

        The returned polygon can be used for spatial filtering to search for features
        of interest within the provided Isochrone.



        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_range_request: Request body of RouteRange API in GeoJSON format. Required.
        :type route_range_request: JSON
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :return: RouteRangeResponse. The RouteRangeResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_range(
        self,
        route_range_request: IO[bytes],
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        **kwargs: Any
    ) -> _models.RouteRangeResponse:
        """The Route Range API creates a polygon that depicts the area reachable from a
        given location within a certain threshold based on the specified time or
        distance budget. A polygon boundary (or Isochrone) is returned in a
        counterclockwise orientation as well as the precise polygon center which was
        the result of the origin point.

        The returned polygon can be used for spatial filtering to search for features
        of interest within the provided Isochrone.



        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_range_request: Request body of RouteRange API in GeoJSON format. Required.
        :type route_range_request: IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :return: RouteRangeResponse. The RouteRangeResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def post_route_range(
        self,
        route_range_request: Union[_models.RouteRangeRequest, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> _models.RouteRangeResponse:
        """The Route Range API creates a polygon that depicts the area reachable from a
        given location within a certain threshold based on the specified time or
        distance budget. A polygon boundary (or Isochrone) is returned in a
        counterclockwise orientation as well as the precise polygon center which was
        the result of the origin point.

        The returned polygon can be used for spatial filtering to search for features
        of interest within the provided Isochrone.



        For information about routing availability in countries/regions, see `Azure
        Maps routing
        coverage <https://learn.microsoft.com/azure/azure-maps/routing-coverage?pivots=latest>`_.

        :param route_range_request: Request body of RouteRange API in GeoJSON format. Is one of the
         following types: RouteRangeRequest, JSON, IO[bytes] Required.
        :type route_range_request: ~azure.maps.route.models.RouteRangeRequest or JSON or IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: RouteRangeResponse. The RouteRangeResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.RouteRangeResponse] = kwargs.pop("cls", None)

        content_type = content_type or "application/geo+json"
        _content = None
        if isinstance(route_range_request, (IOBase, bytes)):
            _content = route_range_request
        else:
            _content = json.dumps(route_range_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_route_range_request(
            client_id=client_id,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        response_headers = {}
        response_headers["Content-Type"] = self._deserialize("str", response.headers.get("Content-Type"))

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.RouteRangeResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, response_headers)  # type: ignore

        return deserialized  # type: ignore

    @overload
    def post_route_range_batch(
        self,
        route_range_batch_request: _models.RouteRangeBatchRequestBody,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.RouteRangeBatchResponse:
        """The Route Range Batch API sends batches of up to **100** queries as a single
        call to the `Route Range
        API
        <https://learn.microsoft.com/rest/api/maps/route/post-route-range?view=rest-maps-2025-01-01>`_.
        This API enables efficient processing of multiple queries in one request,
        reducing the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/range:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *route range* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *route_range* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     5.86605,
                     50.9745
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 6000
                 }
               },
               {
                 "optionalId": "300af787-589b-4088-accd-56828542e293",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     -122.201669,
                     47.615076
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 2000
                 }
               }
             ]
           }

        A *route range* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`RouteRangeResponse`](/rest/api/maps/route/post-route-range#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_range_batch_request: The list of route directions queries/requests to process. The
         list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Required.
        :type route_range_batch_request: ~azure.maps.route.models.RouteRangeBatchRequestBody
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: RouteRangeBatchResponse. The RouteRangeBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_range_batch(
        self,
        route_range_batch_request: JSON,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.RouteRangeBatchResponse:
        """The Route Range Batch API sends batches of up to **100** queries as a single
        call to the `Route Range
        API
        <https://learn.microsoft.com/rest/api/maps/route/post-route-range?view=rest-maps-2025-01-01>`_.
        This API enables efficient processing of multiple queries in one request,
        reducing the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/range:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *route range* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *route_range* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     5.86605,
                     50.9745
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 6000
                 }
               },
               {
                 "optionalId": "300af787-589b-4088-accd-56828542e293",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     -122.201669,
                     47.615076
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 2000
                 }
               }
             ]
           }

        A *route range* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`RouteRangeResponse`](/rest/api/maps/route/post-route-range#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_range_batch_request: The list of route directions queries/requests to process. The
         list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Required.
        :type route_range_batch_request: JSON
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: RouteRangeBatchResponse. The RouteRangeBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_range_batch(
        self,
        route_range_batch_request: IO[bytes],
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/json",
        **kwargs: Any
    ) -> _models.RouteRangeBatchResponse:
        """The Route Range Batch API sends batches of up to **100** queries as a single
        call to the `Route Range
        API
        <https://learn.microsoft.com/rest/api/maps/route/post-route-range?view=rest-maps-2025-01-01>`_.
        This API enables efficient processing of multiple queries in one request,
        reducing the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/range:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *route range* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *route_range* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     5.86605,
                     50.9745
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 6000
                 }
               },
               {
                 "optionalId": "300af787-589b-4088-accd-56828542e293",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     -122.201669,
                     47.615076
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 2000
                 }
               }
             ]
           }

        A *route range* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`RouteRangeResponse`](/rest/api/maps/route/post-route-range#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_range_batch_request: The list of route directions queries/requests to process. The
         list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Required.
        :type route_range_batch_request: IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/json".
        :paramtype content_type: str
        :return: RouteRangeBatchResponse. The RouteRangeBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def post_route_range_batch(
        self,
        route_range_batch_request: Union[_models.RouteRangeBatchRequestBody, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> _models.RouteRangeBatchResponse:
        """The Route Range Batch API sends batches of up to **100** queries as a single
        call to the `Route Range
        API
        <https://learn.microsoft.com/rest/api/maps/route/post-route-range?view=rest-maps-2025-01-01>`_.
        This API enables efficient processing of multiple queries in one request,
        reducing the need for individual calls.

        ================================
        Submit Synchronous Batch Request
        ================================

        The Synchronous API is recommended for lightweight batch requests. When the
        service receives a request, it will respond as soon as the batch items are
        calculated and there will be no possibility to retrieve the results later. The
        Synchronous API will return a timeout error (a 408 response) if the request
        takes longer than 60 seconds. The number of batch items is limited to **100**
        for this API.

        .. code-block::

           POST
        https://atlas.microsoft.com/route/range:batch?api-version=2025-01-01===========================
        POST Body for Batch Request
        ===========================

        To send the *route range* queries you will use a ``POST`` request where the
        request body will contain the ``batchItems`` array in ``json`` format and the
        ``Content-Type`` header will be set to ``application/json``. Here's a sample
        request body containing 2 *route_range* queries:




        .. code-block::

           {
             "batchItems": [
               {
                 "optionalId": "bbc9c0f6-ab52-49d8-a788-a658fa654c94",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     5.86605,
                     50.9745
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 6000
                 }
               },
               {
                 "optionalId": "300af787-589b-4088-accd-56828542e293",
                 "type": "Feature",
                 "geometry": {
                   "type": "Point",
                   "coordinates": [
                     -122.201669,
                     47.615076
                   ]
                 },
                 "properties": {
                   "timeBudgetInSec": 2000
                 }
               }
             ]
           }

        A *route range* batchItem object can accept any of the supported *snap to
        roads* `Request
        body </rest/api/maps/route/post-snap-to-roads?view=rest-maps-2025-01-01#request-body>`_



        The batch should contain at least **1** query.


        ====================
        Batch Response Model
        ====================

        The batch response contains a ``summary`` component that indicates the
        ``totalRequests`` that were part of the original batch request and
        ``successfulRequests`` i.e. queries which were executed successfully. The batch
        response also includes a ``batchItems`` array which contains a response for each
        and every query in the batch request. The ``batchItems`` will contain the results
        in the exact same order the original queries were sent in the batch request.
        Each item is of one of the following types:



        * [`RouteRangeResponse`](/rest/api/maps/route/post-route-range#response) - If
        the query completed successfully.

        * `Error` - If the query failed. The response will contain a `code` and a
        `message` in this case.

        :param route_range_batch_request: The list of route directions queries/requests to process. The
         list can contain
         a max of 100 queries for sync version and must contain at least 1 query. Is one of the
         following types: RouteRangeBatchRequestBody, JSON, IO[bytes] Required.
        :type route_range_batch_request: ~azure.maps.route.models.RouteRangeBatchRequestBody or JSON or
         IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: RouteRangeBatchResponse. The RouteRangeBatchResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteRangeBatchResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.RouteRangeBatchResponse] = kwargs.pop("cls", None)

        content_type = content_type or "application/json"
        _content = None
        if isinstance(route_range_batch_request, (IOBase, bytes)):
            _content = route_range_batch_request
        else:
            _content = json.dumps(route_range_batch_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_route_range_batch_request(
            client_id=client_id,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.RouteRangeBatchResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @overload
    def post_route_matrix(
        self,
        route_matrix_request: _models.RouteMatrixRequest,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        **kwargs: Any
    ) -> _models.RouteMatrixResponse:
        """The ``Route Matrix`` API is an HTTP ``POST`` request that allows calculation of a
        matrix of route summaries for a set of routes defined by origin and destination
        locations by using a sync request. For every given origin, the service
        calculates the cost of routing from that origin to every given destination. The
        set of origins and the set of destinations can be thought of as the column and
        row headers of a table and each cell in the table contains the costs of routing
        from the origin to the destination for that cell. Route Matrices can be
        calculated for driving, walking and truck routes. For example, a food delivery
        company has 20 drivers and they need to find the closest driver to pick up the
        delivery from the restaurant. To solve this use case, they can call the Route
        Matrix API and use the travel cost to sort the drivers by their actual travel
        distance or time from the restaurant.


        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for sync request it's **2500** (the number of
        origins multiplied by the number of destinations).



        =======================================
        Submit Synchronous Route Matrix Request
        =======================================

        If your scenario requires synchronous requests and the maximum size of the
        matrix is less than or equal to 2500, you might want to make synchronous
        request. The maximum size of a matrix for this API is **2500** (the number of
        origins multiplied by the number of destinations). With that constraint in
        mind, examples of possible matrix dimensions are: 50x50, 60x40, 90x20 (it does
        not need to be square).



        ===============
        API Limitations
        ===============

        The synchronous processing of matrix is best suited for fast, small matrices
        of route calculation. To calculate larger matrices and heavy route calculation,
        use the asynchronous endpoint. The following limitation is applicable to the
        synchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size | Max number of origins  | Max number of destinations |
        Additional limits

        |------------------|------------------------|----------------------------|------------|
        | 100              | 100         | 100              | N/A |
        | 200              | 200         | 200              | All origins and
        destinations should be contained in an axis-aligned 400 km x 400 km bounding
        box. Otherwise, some matrix cells will be resolved as OUT_OF_REGION.  |
        | 2500             | 1000        | 1000             | - ``departAt`` or
        ``arriveAt`` must be any.<br> - ``traffic`` must be historical.<br> - ``travelMode``
        must be either driving or truck<br> - No other parameters can be used
        explicitly   |


        Examples:

        * Request of 10x20 matrix with `traffic=live`: This request will be processed
        with a bounding box limit, as it matches a limit of up to 200, which includes
        bounding box restrictions.

        * Request of 10x20 matrix with default parameters (`traffic=historical`): This
        request will be processed without a bounding box limit, as it matches a limit
        of up to 2500, which does  not impose bounding box restrictions.

        :param route_matrix_request: Request body of RouteMatrix API in GeoJSON format. Required.
        :type route_matrix_request: ~azure.maps.route.models.RouteMatrixRequest
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :return: RouteMatrixResponse. The RouteMatrixResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteMatrixResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_matrix(
        self,
        route_matrix_request: JSON,
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        **kwargs: Any
    ) -> _models.RouteMatrixResponse:
        """The ``Route Matrix`` API is an HTTP ``POST`` request that allows calculation of a
        matrix of route summaries for a set of routes defined by origin and destination
        locations by using a sync request. For every given origin, the service
        calculates the cost of routing from that origin to every given destination. The
        set of origins and the set of destinations can be thought of as the column and
        row headers of a table and each cell in the table contains the costs of routing
        from the origin to the destination for that cell. Route Matrices can be
        calculated for driving, walking and truck routes. For example, a food delivery
        company has 20 drivers and they need to find the closest driver to pick up the
        delivery from the restaurant. To solve this use case, they can call the Route
        Matrix API and use the travel cost to sort the drivers by their actual travel
        distance or time from the restaurant.


        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for sync request it's **2500** (the number of
        origins multiplied by the number of destinations).



        =======================================
        Submit Synchronous Route Matrix Request
        =======================================

        If your scenario requires synchronous requests and the maximum size of the
        matrix is less than or equal to 2500, you might want to make synchronous
        request. The maximum size of a matrix for this API is **2500** (the number of
        origins multiplied by the number of destinations). With that constraint in
        mind, examples of possible matrix dimensions are: 50x50, 60x40, 90x20 (it does
        not need to be square).



        ===============
        API Limitations
        ===============

        The synchronous processing of matrix is best suited for fast, small matrices
        of route calculation. To calculate larger matrices and heavy route calculation,
        use the asynchronous endpoint. The following limitation is applicable to the
        synchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size | Max number of origins  | Max number of destinations |
        Additional limits

        |------------------|------------------------|----------------------------|------------|
        | 100              | 100         | 100              | N/A |
        | 200              | 200         | 200              | All origins and
        destinations should be contained in an axis-aligned 400 km x 400 km bounding
        box. Otherwise, some matrix cells will be resolved as OUT_OF_REGION.  |
        | 2500             | 1000        | 1000             | - ``departAt`` or
        ``arriveAt`` must be any.<br> - ``traffic`` must be historical.<br> - ``travelMode``
        must be either driving or truck<br> - No other parameters can be used
        explicitly   |


        Examples:

        * Request of 10x20 matrix with `traffic=live`: This request will be processed
        with a bounding box limit, as it matches a limit of up to 200, which includes
        bounding box restrictions.

        * Request of 10x20 matrix with default parameters (`traffic=historical`): This
        request will be processed without a bounding box limit, as it matches a limit
        of up to 2500, which does  not impose bounding box restrictions.

        :param route_matrix_request: Request body of RouteMatrix API in GeoJSON format. Required.
        :type route_matrix_request: JSON
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :return: RouteMatrixResponse. The RouteMatrixResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteMatrixResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def post_route_matrix(
        self,
        route_matrix_request: IO[bytes],
        *,
        client_id: Optional[str] = None,
        content_type: str = "application/geo+json",
        **kwargs: Any
    ) -> _models.RouteMatrixResponse:
        """The ``Route Matrix`` API is an HTTP ``POST`` request that allows calculation of a
        matrix of route summaries for a set of routes defined by origin and destination
        locations by using a sync request. For every given origin, the service
        calculates the cost of routing from that origin to every given destination. The
        set of origins and the set of destinations can be thought of as the column and
        row headers of a table and each cell in the table contains the costs of routing
        from the origin to the destination for that cell. Route Matrices can be
        calculated for driving, walking and truck routes. For example, a food delivery
        company has 20 drivers and they need to find the closest driver to pick up the
        delivery from the restaurant. To solve this use case, they can call the Route
        Matrix API and use the travel cost to sort the drivers by their actual travel
        distance or time from the restaurant.


        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for sync request it's **2500** (the number of
        origins multiplied by the number of destinations).



        =======================================
        Submit Synchronous Route Matrix Request
        =======================================

        If your scenario requires synchronous requests and the maximum size of the
        matrix is less than or equal to 2500, you might want to make synchronous
        request. The maximum size of a matrix for this API is **2500** (the number of
        origins multiplied by the number of destinations). With that constraint in
        mind, examples of possible matrix dimensions are: 50x50, 60x40, 90x20 (it does
        not need to be square).



        ===============
        API Limitations
        ===============

        The synchronous processing of matrix is best suited for fast, small matrices
        of route calculation. To calculate larger matrices and heavy route calculation,
        use the asynchronous endpoint. The following limitation is applicable to the
        synchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size | Max number of origins  | Max number of destinations |
        Additional limits

        |------------------|------------------------|----------------------------|------------|
        | 100              | 100         | 100              | N/A |
        | 200              | 200         | 200              | All origins and
        destinations should be contained in an axis-aligned 400 km x 400 km bounding
        box. Otherwise, some matrix cells will be resolved as OUT_OF_REGION.  |
        | 2500             | 1000        | 1000             | - ``departAt`` or
        ``arriveAt`` must be any.<br> - ``traffic`` must be historical.<br> - ``travelMode``
        must be either driving or truck<br> - No other parameters can be used
        explicitly   |


        Examples:

        * Request of 10x20 matrix with `traffic=live`: This request will be processed
        with a bounding box limit, as it matches a limit of up to 200, which includes
        bounding box restrictions.

        * Request of 10x20 matrix with default parameters (`traffic=historical`): This
        request will be processed without a bounding box limit, as it matches a limit
        of up to 2500, which does  not impose bounding box restrictions.

        :param route_matrix_request: Request body of RouteMatrix API in GeoJSON format. Required.
        :type route_matrix_request: IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :return: RouteMatrixResponse. The RouteMatrixResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteMatrixResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def post_route_matrix(
        self,
        route_matrix_request: Union[_models.RouteMatrixRequest, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> _models.RouteMatrixResponse:
        """The ``Route Matrix`` API is an HTTP ``POST`` request that allows calculation of a
        matrix of route summaries for a set of routes defined by origin and destination
        locations by using a sync request. For every given origin, the service
        calculates the cost of routing from that origin to every given destination. The
        set of origins and the set of destinations can be thought of as the column and
        row headers of a table and each cell in the table contains the costs of routing
        from the origin to the destination for that cell. Route Matrices can be
        calculated for driving, walking and truck routes. For example, a food delivery
        company has 20 drivers and they need to find the closest driver to pick up the
        delivery from the restaurant. To solve this use case, they can call the Route
        Matrix API and use the travel cost to sort the drivers by their actual travel
        distance or time from the restaurant.


        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for sync request it's **2500** (the number of
        origins multiplied by the number of destinations).



        =======================================
        Submit Synchronous Route Matrix Request
        =======================================

        If your scenario requires synchronous requests and the maximum size of the
        matrix is less than or equal to 2500, you might want to make synchronous
        request. The maximum size of a matrix for this API is **2500** (the number of
        origins multiplied by the number of destinations). With that constraint in
        mind, examples of possible matrix dimensions are: 50x50, 60x40, 90x20 (it does
        not need to be square).



        ===============
        API Limitations
        ===============

        The synchronous processing of matrix is best suited for fast, small matrices
        of route calculation. To calculate larger matrices and heavy route calculation,
        use the asynchronous endpoint. The following limitation is applicable to the
        synchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size | Max number of origins  | Max number of destinations |
        Additional limits

        |------------------|------------------------|----------------------------|------------|
        | 100              | 100         | 100              | N/A |
        | 200              | 200         | 200              | All origins and
        destinations should be contained in an axis-aligned 400 km x 400 km bounding
        box. Otherwise, some matrix cells will be resolved as OUT_OF_REGION.  |
        | 2500             | 1000        | 1000             | - ``departAt`` or
        ``arriveAt`` must be any.<br> - ``traffic`` must be historical.<br> - ``travelMode``
        must be either driving or truck<br> - No other parameters can be used
        explicitly   |


        Examples:

        * Request of 10x20 matrix with `traffic=live`: This request will be processed
        with a bounding box limit, as it matches a limit of up to 200, which includes
        bounding box restrictions.

        * Request of 10x20 matrix with default parameters (`traffic=historical`): This
        request will be processed without a bounding box limit, as it matches a limit
        of up to 2500, which does  not impose bounding box restrictions.

        :param route_matrix_request: Request body of RouteMatrix API in GeoJSON format. Is one of the
         following types: RouteMatrixRequest, JSON, IO[bytes] Required.
        :type route_matrix_request: ~azure.maps.route.models.RouteMatrixRequest or JSON or IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: RouteMatrixResponse. The RouteMatrixResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteMatrixResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.RouteMatrixResponse] = kwargs.pop("cls", None)

        content_type = content_type or "application/geo+json"
        _content = None
        if isinstance(route_matrix_request, (IOBase, bytes)):
            _content = route_matrix_request
        else:
            _content = json.dumps(route_matrix_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_route_matrix_request(
            client_id=client_id,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        response_headers = {}
        response_headers["Content-Type"] = self._deserialize("str", response.headers.get("Content-Type"))

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.RouteMatrixResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, response_headers)  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace
    def get_route_operations_status(self, id: str, **kwargs: Any) -> _models.RouteOperation:
        """Get the status of an asynchronous operation by its operation ID.

        :param id: System generated unique identifier for the asynchronous operation after it has
         been submitted. Required.
        :type id: str
        :return: RouteOperation. The RouteOperation is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteOperation
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.RouteOperation] = kwargs.pop("cls", None)

        _request = build_route_get_route_operations_status_request(
            id=id,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = None
            if response.status_code == 404:
                error = _failsafe_deserialize(_models.MapsErrorResponse, response)
                raise ResourceNotFoundError(response=response, model=error)
            else:
                error = _failsafe_deserialize(
                    _models.MapsErrorResponse,
                    response,
                )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.RouteOperation, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace
    def get_route_operations_result(
        self, id: str, *, client_id: Optional[str] = None, accept_language: Optional[str] = None, **kwargs: Any
    ) -> _models.RouteOperationResponse:
        """Get the result of an asynchronous operation by its operation ID.

        :param id: System generated unique identifier for the asynchronous operation after it has
         been submitted. Required.
        :type id: str
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword accept_language: Language in which routing results should be returned.

         For more information, see `Localization support in Azure
         Maps
         <https://learn.microsoft.com/azure/azure-maps/supported-languages#routing-v2-services-preview-supported-languages>`_.
         Default value is None.
        :paramtype accept_language: str
        :return: RouteOperationResponse. The RouteOperationResponse is compatible with MutableMapping
        :rtype: ~azure.maps.route.models.RouteOperationResponse
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.RouteOperationResponse] = kwargs.pop("cls", None)

        _request = build_route_get_route_operations_result_request(
            id=id,
            client_id=client_id,
            accept_language=accept_language,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = None
            if response.status_code == 404:
                error = _failsafe_deserialize(_models.MapsErrorResponse, response)
                raise ResourceNotFoundError(response=response, model=error)
            else:
                error = _failsafe_deserialize(
                    _models.MapsErrorResponse,
                    response,
                )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.RouteOperationResponse, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    def _post_route_matrix_async_initial(
        self,
        route_matrix_async_request: Union[_models.RouteMatrixAsyncRequest, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> Iterator[bytes]:
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[Iterator[bytes]] = kwargs.pop("cls", None)

        content_type = content_type or "application/geo+json"
        _content = None
        if isinstance(route_matrix_async_request, (IOBase, bytes)):
            _content = route_matrix_async_request
        else:
            _content = json.dumps(route_matrix_async_request, cls=SdkJSONEncoder, exclude_readonly=True)  # type: ignore

        _request = build_route_post_route_matrix_async_request(
            client_id=client_id,
            content_type=content_type,
            api_version=self._config.api_version,
            content=_content,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = True
        pipeline_response: PipelineResponse = self._client._pipeline.run(  # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [202]:
            try:
                response.read()  # Load the body in memory and close the socket
            except (StreamConsumedError, StreamClosedError):
                pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.MapsErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        response_headers = {}
        response_headers["Operation-Location"] = self._deserialize("str", response.headers.get("Operation-Location"))

        deserialized = response.iter_bytes() if _decompress else response.iter_raw()

        if cls:
            return cls(pipeline_response, deserialized, response_headers)  # type: ignore

        return deserialized  # type: ignore

    @overload
    def begin_post_route_matrix_async(
        self,
        route_matrix_async_request: _models.RouteMatrixAsyncRequest,
        *,
        content_type: str = "application/geo+json",
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> LROPoller[_models.RouteOperationResponse]:
        """The ``Route Matrix Async`` API is an HTTP ``POST`` request that allows calculation
        of a matrix of route summaries for a set of routes defined by origin and
        destination locations by using an asynchronous (async) request. For every given
        origin, the service calculates the cost of routing from that origin to every
        given destination. The set of origins and the set of destinations can be
        thought of as the column and row headers of a table and each cell in the table
        contains the costs of routing from the origin to the destination for that cell.
        Route Matrices can be calculated for driving, walking and truck routes.

        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for async request is **50000** (the number of
        origins multiplied by the number of destinations).


        ========================================
        Submit Asynchronous Route Matrix Request
        ========================================

        The Asynchronous API is appropriate for processing big volumes of relatively
        complex routing requests. When you make a request by using async request, by
        default the service returns a 202 response code along a URL in the
        ``operation-Location`` field of the response header with the Azure Maps geography
        endpoint {geography}.atlas.microsoft.com. This URL should be checked
        periodically until the status is Succeeded.


        The maximum size of a matrix for this API is **50000** (the number of origins
        multiplied by the number of destinations). With that constraint in mind,
        examples of possible matrix dimensions are: 500x100, 100x100, 280x170. 100x50
        (it does not need to be square).


        The asynchronous responses are stored for **24** hours. The redirect URL
        returns a 404 response if used after the expiration period.






        .. code-block::

           POST
        https://atlas.microsoft.com/route/matrix:async?api-version=2025-01-01&subscription-key={subscription-key}

        Here's a typical sequence of asynchronous operations:

        1. Client sends a Route Matrix POST request to Azure Maps

        2. The server will respond with one of the following:

        HTTP `202 Accepted` -  Route Matrix request has been accepted.



        HTTP `Error` - There was an error processing your Route Matrix request.
        This could either be a 400 Bad Request or any other Error status code.






        1. If the Matrix Route request was accepted successfully, the
        `operation-location` header in the response contains the URL to get the status
        of the request. This status URI looks like the following:



        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}?api-version=2025-01-01?subscription-key={subscription-key}




        1. Client issues a GET request on the resultUrl obtained in Step 3 to get the
        results





        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}/result?api-version=2025-01-01?subscription-key={subscription-key}



        ===============
        API Limitations
        ===============

        The asynchronous processing of matrix is best suited for larger matrices that
        require heavy route calculation. The following limitation is applicable to the
        asynchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size  | Max number of origins | Max number of destinations  |
        Additional limits |

        |------------------|-----------------------|-----------------------------|-------------------|
        | 2500             | 1000                  | 1000                        | All
        origins and destinations should be contained in an axis-aligned 400 km x 400 km
        bounding box. Otherwise some matrix cells will be resolved as OUT_OF_REGION.  |
        | 50,000           | 10,000                | 10,000                      | -
        ``departAt`` or ``arriveAt`` must be any.<br>- ``traffic`` must be historical.<br/>-
        ``optimizeRoute`` must be fastest.<br/>- ``travelMode`` must be
        either driving or truck. <br/>- No other parameters can be used explicitly.  |.

        :param route_matrix_async_request: Request body of RouteMatrix API in GeoJSON format. Required.
        :type route_matrix_async_request: ~azure.maps.route.models.RouteMatrixAsyncRequest
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: An instance of LROPoller that returns RouteOperationResponse. The
         RouteOperationResponse is compatible with MutableMapping
        :rtype: ~azure.core.polling.LROPoller[~azure.maps.route.models.RouteOperationResponse]
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def begin_post_route_matrix_async(
        self,
        route_matrix_async_request: JSON,
        *,
        content_type: str = "application/geo+json",
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> LROPoller[_models.RouteOperationResponse]:
        """The ``Route Matrix Async`` API is an HTTP ``POST`` request that allows calculation
        of a matrix of route summaries for a set of routes defined by origin and
        destination locations by using an asynchronous (async) request. For every given
        origin, the service calculates the cost of routing from that origin to every
        given destination. The set of origins and the set of destinations can be
        thought of as the column and row headers of a table and each cell in the table
        contains the costs of routing from the origin to the destination for that cell.
        Route Matrices can be calculated for driving, walking and truck routes.

        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for async request is **50000** (the number of
        origins multiplied by the number of destinations).


        ========================================
        Submit Asynchronous Route Matrix Request
        ========================================

        The Asynchronous API is appropriate for processing big volumes of relatively
        complex routing requests. When you make a request by using async request, by
        default the service returns a 202 response code along a URL in the
        ``operation-Location`` field of the response header with the Azure Maps geography
        endpoint {geography}.atlas.microsoft.com. This URL should be checked
        periodically until the status is Succeeded.


        The maximum size of a matrix for this API is **50000** (the number of origins
        multiplied by the number of destinations). With that constraint in mind,
        examples of possible matrix dimensions are: 500x100, 100x100, 280x170. 100x50
        (it does not need to be square).


        The asynchronous responses are stored for **24** hours. The redirect URL
        returns a 404 response if used after the expiration period.






        .. code-block::

           POST
        https://atlas.microsoft.com/route/matrix:async?api-version=2025-01-01&subscription-key={subscription-key}

        Here's a typical sequence of asynchronous operations:

        1. Client sends a Route Matrix POST request to Azure Maps

        2. The server will respond with one of the following:

        HTTP `202 Accepted` -  Route Matrix request has been accepted.



        HTTP `Error` - There was an error processing your Route Matrix request.
        This could either be a 400 Bad Request or any other Error status code.






        1. If the Matrix Route request was accepted successfully, the
        `operation-location` header in the response contains the URL to get the status
        of the request. This status URI looks like the following:



        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}?api-version=2025-01-01?subscription-key={subscription-key}




        1. Client issues a GET request on the resultUrl obtained in Step 3 to get the
        results





        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}/result?api-version=2025-01-01?subscription-key={subscription-key}



        ===============
        API Limitations
        ===============

        The asynchronous processing of matrix is best suited for larger matrices that
        require heavy route calculation. The following limitation is applicable to the
        asynchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size  | Max number of origins | Max number of destinations  |
        Additional limits |

        |------------------|-----------------------|-----------------------------|-------------------|
        | 2500             | 1000                  | 1000                        | All
        origins and destinations should be contained in an axis-aligned 400 km x 400 km
        bounding box. Otherwise some matrix cells will be resolved as OUT_OF_REGION.  |
        | 50,000           | 10,000                | 10,000                      | -
        ``departAt`` or ``arriveAt`` must be any.<br>- ``traffic`` must be historical.<br/>-
        ``optimizeRoute`` must be fastest.<br/>- ``travelMode`` must be
        either driving or truck. <br/>- No other parameters can be used explicitly.  |.

        :param route_matrix_async_request: Request body of RouteMatrix API in GeoJSON format. Required.
        :type route_matrix_async_request: JSON
        :keyword content_type: Body Parameter content-type. Content type parameter for JSON body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: An instance of LROPoller that returns RouteOperationResponse. The
         RouteOperationResponse is compatible with MutableMapping
        :rtype: ~azure.core.polling.LROPoller[~azure.maps.route.models.RouteOperationResponse]
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @overload
    def begin_post_route_matrix_async(
        self,
        route_matrix_async_request: IO[bytes],
        *,
        content_type: str = "application/geo+json",
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> LROPoller[_models.RouteOperationResponse]:
        """The ``Route Matrix Async`` API is an HTTP ``POST`` request that allows calculation
        of a matrix of route summaries for a set of routes defined by origin and
        destination locations by using an asynchronous (async) request. For every given
        origin, the service calculates the cost of routing from that origin to every
        given destination. The set of origins and the set of destinations can be
        thought of as the column and row headers of a table and each cell in the table
        contains the costs of routing from the origin to the destination for that cell.
        Route Matrices can be calculated for driving, walking and truck routes.

        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for async request is **50000** (the number of
        origins multiplied by the number of destinations).


        ========================================
        Submit Asynchronous Route Matrix Request
        ========================================

        The Asynchronous API is appropriate for processing big volumes of relatively
        complex routing requests. When you make a request by using async request, by
        default the service returns a 202 response code along a URL in the
        ``operation-Location`` field of the response header with the Azure Maps geography
        endpoint {geography}.atlas.microsoft.com. This URL should be checked
        periodically until the status is Succeeded.


        The maximum size of a matrix for this API is **50000** (the number of origins
        multiplied by the number of destinations). With that constraint in mind,
        examples of possible matrix dimensions are: 500x100, 100x100, 280x170. 100x50
        (it does not need to be square).


        The asynchronous responses are stored for **24** hours. The redirect URL
        returns a 404 response if used after the expiration period.






        .. code-block::

           POST
        https://atlas.microsoft.com/route/matrix:async?api-version=2025-01-01&subscription-key={subscription-key}

        Here's a typical sequence of asynchronous operations:

        1. Client sends a Route Matrix POST request to Azure Maps

        2. The server will respond with one of the following:

        HTTP `202 Accepted` -  Route Matrix request has been accepted.



        HTTP `Error` - There was an error processing your Route Matrix request.
        This could either be a 400 Bad Request or any other Error status code.






        1. If the Matrix Route request was accepted successfully, the
        `operation-location` header in the response contains the URL to get the status
        of the request. This status URI looks like the following:



        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}?api-version=2025-01-01?subscription-key={subscription-key}




        1. Client issues a GET request on the resultUrl obtained in Step 3 to get the
        results





        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}/result?api-version=2025-01-01?subscription-key={subscription-key}



        ===============
        API Limitations
        ===============

        The asynchronous processing of matrix is best suited for larger matrices that
        require heavy route calculation. The following limitation is applicable to the
        asynchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size  | Max number of origins | Max number of destinations  |
        Additional limits |

        |------------------|-----------------------|-----------------------------|-------------------|
        | 2500             | 1000                  | 1000                        | All
        origins and destinations should be contained in an axis-aligned 400 km x 400 km
        bounding box. Otherwise some matrix cells will be resolved as OUT_OF_REGION.  |
        | 50,000           | 10,000                | 10,000                      | -
        ``departAt`` or ``arriveAt`` must be any.<br>- ``traffic`` must be historical.<br/>-
        ``optimizeRoute`` must be fastest.<br/>- ``travelMode`` must be
        either driving or truck. <br/>- No other parameters can be used explicitly.  |.

        :param route_matrix_async_request: Request body of RouteMatrix API in GeoJSON format. Required.
        :type route_matrix_async_request: IO[bytes]
        :keyword content_type: Body Parameter content-type. Content type parameter for binary body.
         Default value is "application/geo+json".
        :paramtype content_type: str
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: An instance of LROPoller that returns RouteOperationResponse. The
         RouteOperationResponse is compatible with MutableMapping
        :rtype: ~azure.core.polling.LROPoller[~azure.maps.route.models.RouteOperationResponse]
        :raises ~azure.core.exceptions.HttpResponseError:
        """

    @distributed_trace
    def begin_post_route_matrix_async(
        self,
        route_matrix_async_request: Union[_models.RouteMatrixAsyncRequest, JSON, IO[bytes]],
        *,
        client_id: Optional[str] = None,
        **kwargs: Any
    ) -> LROPoller[_models.RouteOperationResponse]:
        """The ``Route Matrix Async`` API is an HTTP ``POST`` request that allows calculation
        of a matrix of route summaries for a set of routes defined by origin and
        destination locations by using an asynchronous (async) request. For every given
        origin, the service calculates the cost of routing from that origin to every
        given destination. The set of origins and the set of destinations can be
        thought of as the column and row headers of a table and each cell in the table
        contains the costs of routing from the origin to the destination for that cell.
        Route Matrices can be calculated for driving, walking and truck routes.

        Route Matrices are used in several different types of applications, most
        commonly to solve the Traveling Salesman Problem (TSP) and Vehicle Routing
        Problem (VRP). For each origin-destination pair in the matrix, the travel time
        and distance are returned. You can use the computed costs to determine which
        detailed routes to calculate using the Route Directions API.


        The maximum size of a matrix for async request is **50000** (the number of
        origins multiplied by the number of destinations).


        ========================================
        Submit Asynchronous Route Matrix Request
        ========================================

        The Asynchronous API is appropriate for processing big volumes of relatively
        complex routing requests. When you make a request by using async request, by
        default the service returns a 202 response code along a URL in the
        ``operation-Location`` field of the response header with the Azure Maps geography
        endpoint {geography}.atlas.microsoft.com. This URL should be checked
        periodically until the status is Succeeded.


        The maximum size of a matrix for this API is **50000** (the number of origins
        multiplied by the number of destinations). With that constraint in mind,
        examples of possible matrix dimensions are: 500x100, 100x100, 280x170. 100x50
        (it does not need to be square).


        The asynchronous responses are stored for **24** hours. The redirect URL
        returns a 404 response if used after the expiration period.






        .. code-block::

           POST
        https://atlas.microsoft.com/route/matrix:async?api-version=2025-01-01&subscription-key={subscription-key}

        Here's a typical sequence of asynchronous operations:

        1. Client sends a Route Matrix POST request to Azure Maps

        2. The server will respond with one of the following:

        HTTP `202 Accepted` -  Route Matrix request has been accepted.



        HTTP `Error` - There was an error processing your Route Matrix request.
        This could either be a 400 Bad Request or any other Error status code.






        1. If the Matrix Route request was accepted successfully, the
        `operation-location` header in the response contains the URL to get the status
        of the request. This status URI looks like the following:



        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}?api-version=2025-01-01?subscription-key={subscription-key}




        1. Client issues a GET request on the resultUrl obtained in Step 3 to get the
        results





        .. code-block::

               GET
        https://atlas.microsoft.com/route/operations/{id}/result?api-version=2025-01-01?subscription-key={subscription-key}



        ===============
        API Limitations
        ===============

        The asynchronous processing of matrix is best suited for larger matrices that
        require heavy route calculation. The following limitation is applicable to the
        asynchronous requests. If none of the rows in the following table match the
        request's parameters, the request does not meet the requirements and will not
        be processed.

        | Max matrix size  | Max number of origins | Max number of destinations  |
        Additional limits |

        |------------------|-----------------------|-----------------------------|-------------------|
        | 2500             | 1000                  | 1000                        | All
        origins and destinations should be contained in an axis-aligned 400 km x 400 km
        bounding box. Otherwise some matrix cells will be resolved as OUT_OF_REGION.  |
        | 50,000           | 10,000                | 10,000                      | -
        ``departAt`` or ``arriveAt`` must be any.<br>- ``traffic`` must be historical.<br/>-
        ``optimizeRoute`` must be fastest.<br/>- ``travelMode`` must be
        either driving or truck. <br/>- No other parameters can be used explicitly.  |.

        :param route_matrix_async_request: Request body of RouteMatrix API in GeoJSON format. Is one of
         the following types: RouteMatrixAsyncRequest, JSON, IO[bytes] Required.
        :type route_matrix_async_request: ~azure.maps.route.models.RouteMatrixAsyncRequest or JSON or
         IO[bytes]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: An instance of LROPoller that returns RouteOperationResponse. The
         RouteOperationResponse is compatible with MutableMapping
        :rtype: ~azure.core.polling.LROPoller[~azure.maps.route.models.RouteOperationResponse]
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        _headers = case_insensitive_dict(kwargs.pop("headers", {}) or {})
        _params = kwargs.pop("params", {}) or {}

        content_type: Optional[str] = kwargs.pop("content_type", _headers.pop("Content-Type", None))
        cls: ClsType[_models.RouteOperationResponse] = kwargs.pop("cls", None)
        polling: Union[bool, PollingMethod] = kwargs.pop("polling", True)
        lro_delay = kwargs.pop("polling_interval", self._config.polling_interval)
        cont_token: Optional[str] = kwargs.pop("continuation_token", None)
        if cont_token is None:
            raw_result = self._post_route_matrix_async_initial(
                route_matrix_async_request=route_matrix_async_request,
                client_id=client_id,
                content_type=content_type,
                cls=lambda x, y, z: x,
                headers=_headers,
                params=_params,
                **kwargs
            )
            raw_result.http_response.read()  # type: ignore
        kwargs.pop("error_map", None)

        def get_long_running_output(pipeline_response):
            response_headers = {}
            response = pipeline_response.http_response
            response_headers["Operation-Location"] = self._deserialize(
                "str", response.headers.get("Operation-Location")
            )

            deserialized = _deserialize(_models.RouteOperationResponse, response.json())
            if cls:
                return cls(pipeline_response, deserialized, response_headers)  # type: ignore
            return deserialized

        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }

        if polling is True:
            polling_method: PollingMethod = cast(
                PollingMethod, LROBasePolling(lro_delay, path_format_arguments=path_format_arguments, **kwargs)
            )
        elif polling is False:
            polling_method = cast(PollingMethod, NoPolling())
        else:
            polling_method = polling
        if cont_token:
            return LROPoller[_models.RouteOperationResponse].from_continuation_token(
                polling_method=polling_method,
                continuation_token=cont_token,
                client=self._client,
                deserialization_callback=get_long_running_output,
            )
        return LROPoller[_models.RouteOperationResponse](
            self._client, raw_result, get_long_running_output, polling_method  # type: ignore
        )
