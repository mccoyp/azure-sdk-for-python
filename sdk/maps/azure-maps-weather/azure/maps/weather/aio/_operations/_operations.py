# pylint: disable=line-too-long,useless-suppression,too-many-lines
# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
# Code generated by Microsoft (R) Python Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is regenerated.
# --------------------------------------------------------------------------
from collections.abc import MutableMapping
import datetime
from typing import Any, Callable, Optional, TypeVar, Union

from azure.core import AsyncPipelineClient
from azure.core.exceptions import (
    ClientAuthenticationError,
    HttpResponseError,
    ResourceExistsError,
    ResourceNotFoundError,
    ResourceNotModifiedError,
    StreamClosedError,
    StreamConsumedError,
    map_error,
)
from azure.core.pipeline import PipelineResponse
from azure.core.rest import AsyncHttpResponse, HttpRequest
from azure.core.tracing.decorator_async import distributed_trace_async

from ... import models as _models
from ..._operations._operations import (
    build_maps_weather_get_air_quality_daily_forecasts_request,
    build_maps_weather_get_air_quality_hourly_forecasts_request,
    build_maps_weather_get_current_air_quality_request,
    build_maps_weather_get_current_conditions_request,
    build_maps_weather_get_daily_forecast_request,
    build_maps_weather_get_daily_historical_actuals_request,
    build_maps_weather_get_daily_historical_normals_request,
    build_maps_weather_get_daily_historical_records_request,
    build_maps_weather_get_daily_indices_request,
    build_maps_weather_get_hourly_forecast_request,
    build_maps_weather_get_minute_forecast_request,
    build_maps_weather_get_quarter_day_forecast_request,
    build_maps_weather_get_severe_weather_alerts_request,
    build_maps_weather_get_tropical_storm_active_request,
    build_maps_weather_get_tropical_storm_forecast_request,
    build_maps_weather_get_tropical_storm_locations_request,
    build_maps_weather_get_tropical_storm_search_request,
    build_maps_weather_get_weather_along_route_request,
)
from ..._utils.model_base import _deserialize, _failsafe_deserialize
from ..._utils.utils import ClientMixinABC
from .._configuration import MapsWeatherClientConfiguration

T = TypeVar("T")
ClsType = Optional[Callable[[PipelineResponse[HttpRequest, AsyncHttpResponse], T, dict[str, Any]], Any]]


class _MapsWeatherClientOperationsMixin(
    ClientMixinABC[AsyncPipelineClient[HttpRequest, AsyncHttpResponse], MapsWeatherClientConfiguration]
):

    @distributed_trace_async
    async def get_hourly_forecast(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        duration: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.HourlyForecastResult:
        """Use to get a detailed hourly weather forecast for up to 24 hours or a daily forecast for up to
        10 days.

        The ``Get Hourly Forecast`` API is an HTTP ``GET`` that Request detailed weather
        forecast by the hour for the next 1, 12, 24 (1 day), 72 (3 days), 120 (5 days),
        and 240 hours (10 days) for the given the given coordinate location.  The API
        returns details such as temperature, humidity, wind, precipitation, and
        ultraviolet (UV) index. For more information, see `Request hourly weather
        forecast
        data </azure/azure-maps/how-to-request-weather-data#request-hourly-weather-forecast-data>`_.

        If you are using the Gen1 S0 pricing tier, you can request hourly forecast for
        the next 1, 12, 24 hours (1 day), and 72 hours (3 days). If you are using Gen1
        S1 or Gen2 pricing tier, you can also request hourly forecast for the next 120
        (5 days) and 240 hours (10 days).

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :keyword duration: Time frame of the returned weather forecast. By default, the forecast data
         for
         next hour will be returned. Available values are

         * `1` - Return forecast data for the next hour. Default value.
         * `12` - Return hourly forecast for next 12 hours.
         * `24` - Return hourly forecast for next 24 hours.
         * `72` - Return hourly forecast for next 72 hours (3 days).
         * `120` - Return hourly forecast for next 120 hours (5 days). Only available
         in S1 SKU.
         * `240` - Return hourly forecast for next 240 hours (10 days). Only available
         in S1 SKU. Default value is None.
        :paramtype duration: int
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :return: HourlyForecastResult. The HourlyForecastResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.HourlyForecastResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.HourlyForecastResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_hourly_forecast_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            unit=unit,
            duration=duration,
            language=language,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.HourlyForecastResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_minute_forecast(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        interval: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.MinuteForecastResult:
        """Use to get a minute-by-minute forecast for the next 120 minutes in intervals of 1, 5 and 15
        minutes.

        The ``Get Minute Forecast`` API is an HTTP ``GET`` request that returns minute-by-minute
        forecasts for a given location for the next 120 minutes. Users can request weather forecasts in
        intervals of 1, 5 and 15 minutes.  The response will include details such as the type of
        precipitation (including rain, snow, or a mixture of both), start time, and precipitation
        intensity value (dBZ). For more information, see `Request minute-by-minute weather forecast
        data
        </azure/azure-maps/how-to-request-weather-data#request-minute-by-minute-weather-forecast-data>`_.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword interval: Specifies time interval in minutes for the returned weather forecast.
         Supported
         values are

         * `1` -  Retrieve forecast for 1-minute intervals. Returned by default.
         * `5` - Retrieve forecasts for 5-minute intervals.
         * `15` - Retrieve forecasts for 15-minute intervals. Default value is None.
        :paramtype interval: int
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :return: MinuteForecastResult. The MinuteForecastResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.MinuteForecastResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.MinuteForecastResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_minute_forecast_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            interval=interval,
            language=language,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.MinuteForecastResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_quarter_day_forecast(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        duration: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.QuarterDayForecastResult:
        """Use to get a Quarter-Day Forecast for the next 1, 5, 10, or 15 days.

        The ``Get Quarter-Day Forecast`` API is an HTTP ``GET`` request that returns a detailed weather
        forecast by quarter-day for the next 1, 5, 10, or 15 days for a given location. Response data
        is presented by quarters of the day - morning, afternoon, evening, and overnight. Details such
        as temperature, humidity, wind, precipitation, and UV index are returned.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :keyword duration: Specifies for how many days the quester-day forecast responses are returned.
         Supported values are:

         * `1` - Return forecast data for the next day. Returned by default.
         * `5` - Return forecast data for the next 5 days.
         * `10` - Return forecast data for next 10 days.
         * `15` - Return forecast data for the next 15 days. Default value is None.
        :paramtype duration: int
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :return: QuarterDayForecastResult. The QuarterDayForecastResult is compatible with
         MutableMapping
        :rtype: ~azure.maps.weather.models.QuarterDayForecastResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.QuarterDayForecastResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_quarter_day_forecast_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            unit=unit,
            duration=duration,
            language=language,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.QuarterDayForecastResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_current_conditions(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        details: Optional[str] = None,
        duration: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.CurrentConditionsResult:
        """Use to get current weather conditions.

        The ``Get Current Conditions`` API is an HTTP ``GET`` request that returns detailed current
        weather conditions such as precipitation, temperature and wind for a given coordinate location.
        Also, observations from the past 6 or 24 hours for a particular location can be retrieved. The
        basic information returned with The response includes details such as observation date and
        time, brief description of the weather conditions, weather icon, precipitation indicator flags,
        and temperature. Additional details such as RealFeel™ Temperature and UV index are also
        returned. For more information, see `Request real-time weather data
        </azure/azure-maps/how-to-request-weather-data#request-real-time-weather-data>`_.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :keyword details: Return full details for the current conditions. Available values are

         * `true` - Returns full details. By default all details are returned.
         * `false` - Returns a truncated version of the current condition data, which
         includes observation date time, weather phrase, icon code, precipitation
         indicator flag, and temperature. Default value is None.
        :paramtype details: str
        :keyword duration: Time frame of the returned weather conditions. By default, the most current
         weather conditions will be returned. Default value is 0. Supported values are:

         * `0` - Return the most current weather conditions.
         * `6` - Return weather conditions from past 6 hours.
         * `24` - Return weather conditions from past 24 hours. Default value is None.
        :paramtype duration: int
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :return: CurrentConditionsResult. The CurrentConditionsResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.CurrentConditionsResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.CurrentConditionsResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_current_conditions_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            unit=unit,
            details=details,
            duration=duration,
            language=language,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.CurrentConditionsResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_daily_forecast(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        duration: Optional[int] = None,
        language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.DailyForecastResult:
        """Use to get a daily detailed weather forecast for the next 1, 5, 10, 15, 25, or 45 days.

        The ``Get Daily Forecast`` API is an HTTP ``GET`` request that returns detailed
        weather forecast such as temperature and wind by day for the next 1, 5, 10, 15,
        25, or 45 days for a given coordinate location.  The response includes details
        such as temperature, wind, precipitation, air quality, and UV index. For more
        information, see `Request daily weather forecast
        data </azure/azure-maps/how-to-request-weather-data#request-daily-weather-forecast-data>`_.

        If you are using the Gen1 S0 pricing tier, you can request daily forecast for
        the next 1, 5, 10, and 15 days. If you are using Gen1 S1 or Gen2 pricing tier,
        you can also request daily forecast for the next 25 days, and 45 days.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :keyword duration: Specifies for how many days the daily forecast responses are returned.
         Available values are

         * `1` - Return forecast data for the next day. Returned by default.
         * `5` - Return forecast data for the next 5 days.
         * `10` - Return forecast data for the next 10 days.
         * `25` - Return forecast data for the next 25 days. Only available in S1 SKU.
         * `45` - Return forecast data for the next 45 days. Only available in S1 SKU. Default value is
         None.
        :paramtype duration: int
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :return: DailyForecastResult. The DailyForecastResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.DailyForecastResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.DailyForecastResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_daily_forecast_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            unit=unit,
            duration=duration,
            language=language,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.DailyForecastResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_weather_along_route(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        query: str,
        client_id: Optional[str] = None,
        language: Optional[str] = None,
        **kwargs: Any
    ) -> _models.WeatherAlongRouteResult:
        """Use to get a locationally precise, up-to-the-minute forecast that includes weather hazard
        assessments and notifications along a route.

        The ``Get Weather Along Route`` API is an HTTP ``GET`` request that returns hyper
        local (one kilometer or less), up-to-the-minute weather nowcasts, weather
        hazard assessments, and notifications along a route described as a sequence of
        waypoints.
        This includes a list of weather hazards affecting the waypoint or route, and
        the aggregated hazard index for each waypoint might be used to paint each
        portion of a route according to how safe it is for the driver. When submitting
        the waypoints, it is recommended to stay within, or close to, the distance that
        can be traveled within 120-mins or shortly after. Data is updated every five
        minutes.

        The service supplements Azure Maps `Route
        Service <https://docs.microsoft.com/rest/api/maps/route>`_ that allows you to
        first request a route between an origin and a destination and use that as an
        input for Weather Along Route endpoint.

        In addition, the service supports scenarios to generate weather notifications
        for waypoints that experience an increase in intensity of a weather hazard. For
        example, if the vehicle is expected to begin experiencing heavy rain as it
        reaches a waypoint, a weather notification for heavy rain will be generated for
        that waypoint allowing the end product to display a heavy rain notification
        before the driver reaches that waypoint.
        The trigger for when to display the notification for a waypoint could be
        based, for example, on a
        `geofence <https://docs.microsoft.com/azure/azure-maps/tutorial-iot-hub-maps>`_,
        or selectable distance to the waypoint.

        The API covers all regions of the planet except latitudes above Greenland and
        Antarctica.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword query: Coordinates through which the route is calculated, separated by colon (:) and
         entered in chronological order. A minimum of two waypoints is required. A
         single API call may contain up to 60 waypoints.
         A waypoint indicates location, ETA, and optional heading:
         latitude,longitude,ETA,heading, where

         * `Latitude` - Latitude coordinate in decimal degrees.
         * `Longitude` - Longitude coordinate in decimal degrees.
         * `ETA (estimated time of arrival)` - The number of minutes from the present
         time that it will take for the vehicle to reach the waypoint. Allowed range is
         from 0.0 to 120.0 minutes.
         * `Heading` - An optional value indicating the vehicle heading as it passes
         the waypoint. Expressed in clockwise degrees relative to true north. This is
         issued to calculate sun glare as a driving hazard. Allowed range is from 0.0 to
         360.0 degrees. If not provided, a heading will automatically be derived based
         on the position of neighboring waypoints.

         It is recommended to stay within, or close to, the distance that can be
         traveled within 120-mins or shortly after. This way a more accurate assessment
         can be provided for the trip and prevent isolated events not being captured
         between waypoints.  Information can and should be updated along the route
         (especially for trips greater than 2 hours) to continuously pull new waypoints
         moving forward, but also to ensure that forecast information for content such
         as precipitation type and intensity is accurate as storms develop and dissipate
         over time. Required.
        :paramtype query: str
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :return: WeatherAlongRouteResult. The WeatherAlongRouteResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.WeatherAlongRouteResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.WeatherAlongRouteResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_weather_along_route_request(
            format=format,
            query=query,
            client_id=client_id,
            language=language,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.WeatherAlongRouteResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_severe_weather_alerts(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        language: Optional[str] = None,
        details: Optional[str] = None,
        **kwargs: Any
    ) -> _models.SevereWeatherAlertsResult:
        """Use to get information about severe weather conditions such as hurricanes, thunderstorms,
        flooding, lightning, heat waves or forest fires for a given location.

        Severe weather phenomenon can significantly impact our everyday life and
        business operations. For example, severe weather conditions such as tropical
        storms, high winds or flooding can close roads and force logistics companies to
        reroute their fleet causing delays in reaching destinations and breaking the
        cold chain of refrigerated food products.

        The ``Get Severe Weather Alerts`` API is an HTTP ``GET`` request that returns the
        severe weather alerts that are available worldwide from both official
        Government Meteorological Agencies and leading global to regional weather alert
        providers. The service can return details such as alert type, category, level
        and detailed description about the active severe alerts for the requested
        location, like hurricanes, thunderstorms, lightning, heat waves or forest
        fires. For more information, see `Request severe weather
        alerts </azure/azure-maps/how-to-request-weather-data#request-severe-weather-alerts>`_.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :keyword details: Return full details for the severe weather alerts. Available values are

         * `true` - Returns full details. By default all details are returned.
         * `false` - Returns a truncated version of the alerts data, which excludes
         the area-specific full description of alert details (`alertDetails`). Default value is None.
        :paramtype details: str
        :return: SevereWeatherAlertsResult. The SevereWeatherAlertsResult is compatible with
         MutableMapping
        :rtype: ~azure.maps.weather.models.SevereWeatherAlertsResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.SevereWeatherAlertsResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_severe_weather_alerts_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            language=language,
            details=details,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.SevereWeatherAlertsResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_daily_indices(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        language: Optional[str] = None,
        duration: Optional[int] = None,
        index_id: Optional[int] = None,
        index_group_id: Optional[int] = None,
        **kwargs: Any
    ) -> _models.DailyIndicesResult:
        """Use when you want to know if the weather conditions are optimal for a specific activity such as
        outdoor sporting activities, construction, or farming (results includes soil moisture
        information).

        The ``Get Daily Indices`` API is an HTTP ``GET`` request returns index values that provide
        guidance to help when planning future activities. For example, a health mobile application can
        notify users that today is good weather for running or for other outdoors activities like
        playing golf or flying a kite. Retail stores can optimize their digital marketing campaigns
        based on predicted index values. The service returns in daily indices values for current and
        next 5, 10 and 15 days starting from current day.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :keyword duration: Specifies for how many days the daily indices are returned. By default, the
         indices data for the current day will be returned. When requesting future
         indices data, the current day is included in the response as day 1. Available
         values are

         * `1` - Return daily index data for the current day. Default value.
         * `5` - Return 5 days of daily index data starting from the current day.
         * `10` - Return 10 days of daily index data starting from the current day.
         * `15` - Return 15 days of daily index data starting from the current day. Default value is
         None.
        :paramtype duration: int
        :keyword index_id: Numeric index identifier that can be used for restricting returned results
         to
         the corresponding index type. Cannot be paired with ``indexGroupId``. Please
         refer to `Weather services in Azure
         Maps </azure/azure-maps/weather-services-concepts#index-ids-and-index-groups-ids>`_
         for details and to see the supported indices. Default value is None.
        :paramtype index_id: int
        :keyword index_group_id: Numeric index group identifier that can be used for restricting
         returned
         results to the corresponding subset of indices (index group). Cannot be paired
         with ``indexId``. Please refer to `Weather services in Azure
         Maps </azure/azure-maps/weather-services-concepts#index-ids-and-index-groups-ids>`_
         for details and to see the supported index groups. Default value is None.
        :paramtype index_group_id: int
        :return: DailyIndicesResult. The DailyIndicesResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.DailyIndicesResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.DailyIndicesResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_daily_indices_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            language=language,
            duration=duration,
            index_id=index_id,
            index_group_id=index_group_id,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.DailyIndicesResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_tropical_storm_active(
        self, format: Union[str, _models.JsonFormat] = "json", *, client_id: Optional[str] = None, **kwargs: Any
    ) -> _models.ActiveStormResult:
        """Use to get a list of the active tropical storms issued by national weather forecasting
        agencies.

        The ``Get Tropical Storm Active`` API is an HTTP ``GET`` request that returns a list of all
        government-issued active tropical storms. Information about the tropical storms includes,
        government ID, basin ID, year of origin, name and if it is subtropical.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :return: ActiveStormResult. The ActiveStormResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.ActiveStormResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.ActiveStormResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_tropical_storm_active_request(
            format=format,
            client_id=client_id,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.ActiveStormResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_tropical_storm_search(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        year: int,
        client_id: Optional[str] = None,
        basin_id: Optional[Union[str, _models.BasinId]] = None,
        government_storm_id: Optional[int] = None,
        **kwargs: Any
    ) -> _models.StormSearchResult:
        """Use to get a list of storms issued by national weather forecasting agencies.

        The ``Get Tropical Storm Search`` API is an HTTP ``GET`` request that returns a list of
        government-issued tropical storms by year, basin ID, and government ID. Information about the
        tropical storms includes, government ID, basin ID, status, year, name and if it is subtropical.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword year: Year of the cyclone(s). Required.
        :paramtype year: int
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword basin_id: The ``basinId`` enum categorizes the different ocean basins where tropical
         storms
         can originate. Each basin is assigned a unique identifier to help specify the
         location of the storm's origin. Known values are: "AL", "CP", "EP", "NI", "NP", "SI", and
         "SP". Default value is None.
        :paramtype basin_id: str or ~azure.maps.weather.models.BasinId
        :keyword government_storm_id: Government storm Id. Default value is None.
        :paramtype government_storm_id: int
        :return: StormSearchResult. The StormSearchResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.StormSearchResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.StormSearchResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_tropical_storm_search_request(
            format=format,
            year=year,
            client_id=client_id,
            basin_id=basin_id,
            government_storm_id=government_storm_id,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.StormSearchResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_tropical_storm_forecast(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        year: int,
        basin_id: Union[str, _models.BasinId],
        government_storm_id: int,
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        include_details: Optional[bool] = None,
        include_geometric_details: Optional[bool] = None,
        include_window_geometry: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.StormForecastResult:
        """Use to get a list of tropical storms forecasted by national weather forecasting agencies.

        The ``Get Tropical Storm Forecasts`` API is an HTTP ``GET`` request that returns individual
        government-issued tropical storm forecasts. Information about the forecasted tropical storms
        includes, location, status, date the forecast was created, window, wind speed and wind radii.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword year: Year of the cyclone(s). Required.
        :paramtype year: int
        :keyword basin_id: The ``basinId`` enum categorizes the different ocean basins where tropical
         storms
         can originate. Each basin is assigned a unique identifier to help specify the
         location of the storm's origin. Known values are: "AL", "CP", "EP", "NI", "NP", "SI", and
         "SP". Required.
        :paramtype basin_id: str or ~azure.maps.weather.models.BasinId
        :keyword government_storm_id: Government storm Id. Required.
        :paramtype government_storm_id: int
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :keyword include_details: When true, wind radii summary data is included in the response.
         Default value is None.
        :paramtype include_details: bool
        :keyword include_geometric_details: When true, wind radii summary data and geoJSON details are
         included in the
         response. Default value is None.
        :paramtype include_geometric_details: bool
        :keyword include_window_geometry: When true, window geometry data (geoJSON) is included in the
         response. Default value is None.
        :paramtype include_window_geometry: bool
        :return: StormForecastResult. The StormForecastResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.StormForecastResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.StormForecastResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_tropical_storm_forecast_request(
            format=format,
            year=year,
            basin_id=basin_id,
            government_storm_id=government_storm_id,
            client_id=client_id,
            unit=unit,
            include_details=include_details,
            include_geometric_details=include_geometric_details,
            include_window_geometry=include_window_geometry,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.StormForecastResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_tropical_storm_locations(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        year: int,
        basin_id: Union[str, _models.BasinId],
        government_storm_id: int,
        client_id: Optional[str] = None,
        include_details: Optional[bool] = None,
        include_geometric_details: Optional[bool] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        include_current_storm: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.StormLocationsResult:
        """Use to get the location of tropical storms from individual national weather forecasting
        agencies.

        The ``Get Tropical Storm Locations`` API is an HTTP ``GET`` request that returns the location
        of individual government-issued tropical storms. Information about the tropical storms
        includes, location coordinates, geometry, basin ID, date, wind details and wind radii.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword year: Year of the cyclone(s). Required.
        :paramtype year: int
        :keyword basin_id: The ``basinId`` enum categorizes the different ocean basins where tropical
         storms
         can originate. Each basin is assigned a unique identifier to help specify the
         location of the storm's origin. Known values are: "AL", "CP", "EP", "NI", "NP", "SI", and
         "SP". Required.
        :paramtype basin_id: str or ~azure.maps.weather.models.BasinId
        :keyword government_storm_id: Government storm Id. Required.
        :paramtype government_storm_id: int
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword include_details: When true, wind radii summary data is included in the response.
         Default value is None.
        :paramtype include_details: bool
        :keyword include_geometric_details: When true, wind radii summary data and geoJSON details are
         included in the
         response. Default value is None.
        :paramtype include_geometric_details: bool
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :keyword include_current_storm: When true, return the current storm location. Default value is
         None.
        :paramtype include_current_storm: bool
        :return: StormLocationsResult. The StormLocationsResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.StormLocationsResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.StormLocationsResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_tropical_storm_locations_request(
            format=format,
            year=year,
            basin_id=basin_id,
            government_storm_id=government_storm_id,
            client_id=client_id,
            include_details=include_details,
            include_geometric_details=include_geometric_details,
            unit=unit,
            include_current_storm=include_current_storm,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.StormLocationsResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_current_air_quality(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        language: Optional[str] = None,
        include_pollutant_details: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.AirQualityResult:
        """Use to get current air quality information that includes potential risks and suggested
        precautions.

        The ``Get Current Air Quality`` API is an HTTP ``GET`` request that returns detailed
        information about the concentration of pollutants and overall status for current air quality,
        including pollution levels, air quality index values, the dominant pollutant, and a brief
        statement summarizing risk level and suggested precautions.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :keyword include_pollutant_details: Boolean value that returns detailed information about each
         pollutant. By
         default is True. Default value is None.
        :paramtype include_pollutant_details: bool
        :return: AirQualityResult. The AirQualityResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.AirQualityResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.AirQualityResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_current_air_quality_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            language=language,
            include_pollutant_details=include_pollutant_details,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.AirQualityResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_air_quality_daily_forecasts(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        language: Optional[str] = None,
        duration: Optional[Union[int, _models.DailyDuration]] = None,
        **kwargs: Any
    ) -> _models.DailyAirQualityForecastResult:
        """Use to get daily air quality forecasts for the next one to seven days that include pollutant
        levels, potential risks and suggested precautions.

        The ``Get Air Quality Daily Forecasts`` API is an HTTP ``GET`` request that returns detailed
        information about the concentration of pollutants and overall status of forecasted daily air
        quality. The service can provide forecasted daily air quality information for the upcoming 1 to
        7 days, including pollution levels, air quality index values, the dominant pollutant, and a
        brief statement summarizing risk level and suggested precautions.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :keyword duration: Defines the duration for the air quality forecast, with options ranging from
         1
         to 7 days. The default duration is 1 day. Known values are: 1, 2, 3, 4, 5, 6, and 7. Default
         value is None.
        :paramtype duration: int or ~azure.maps.weather.models.DailyDuration
        :return: DailyAirQualityForecastResult. The DailyAirQualityForecastResult is compatible with
         MutableMapping
        :rtype: ~azure.maps.weather.models.DailyAirQualityForecastResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.DailyAirQualityForecastResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_air_quality_daily_forecasts_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            language=language,
            duration=duration,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.DailyAirQualityForecastResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_air_quality_hourly_forecasts(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        client_id: Optional[str] = None,
        language: Optional[str] = None,
        duration: Optional[Union[int, _models.HourlyDuration]] = None,
        include_pollutant_details: Optional[bool] = None,
        **kwargs: Any
    ) -> _models.AirQualityResult:
        """Use to get hourly air quality forecasts for the next one to 96 hours that include pollutant
        levels, potential risks and suggested precautions.

        The ``Get Air Quality Hourly Forecasts`` API is an HTTP ``GET`` request that returns detailed
        information about the concentration of pollutants and overall status for forecasted upcoming
        hourly air quality. The service can provide forecasted hourly air quality information for the
        upcoming time spans of 1, 12, 24, 48, 72, and 96 hours, including pollution levels, air quality
        index values, the dominant pollutant, and a brief statement summarizing risk level and
        suggested precautions.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword language: Language in which search results should be returned. Should be one of
         supported
         IETF language tags, case insensitive. When data in specified language is not
         available for a specific field, default language is used.

         Please refer to `Supported
         Languages <https://docs.microsoft.com/azure/azure-maps/supported-languages>`_ for
         details. Default value is None.
        :paramtype language: str
        :keyword duration: Defines the duration, in hours, for the air quality forecast. Available
         values
         are 1, 12, 24, 48, 72, and 96. The default value is 1 hour. Known values are: 1, 12, 24, 48,
         72, and 96. Default value is None.
        :paramtype duration: int or ~azure.maps.weather.models.HourlyDuration
        :keyword include_pollutant_details: Boolean value that returns detailed information about each
         pollutant. By
         default is True. Default value is None.
        :paramtype include_pollutant_details: bool
        :return: AirQualityResult. The AirQualityResult is compatible with MutableMapping
        :rtype: ~azure.maps.weather.models.AirQualityResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.AirQualityResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_air_quality_hourly_forecasts_request(
            format=format,
            coordinates=coordinates,
            client_id=client_id,
            language=language,
            duration=duration,
            include_pollutant_details=include_pollutant_details,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.AirQualityResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_daily_historical_actuals(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        start_date: datetime.date,
        end_date: datetime.date,
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        **kwargs: Any
    ) -> _models.DailyHistoricalActualsResult:
        """Use to get climatology data such as past daily actual observed temperatures, precipitation,
        snowfall and snow depth.

        The ``Get Daily Historical Actuals`` API is an HTTP ``GET`` request that returns climatology
        data such as past daily actual observed temperatures, precipitation, snowfall, snow depth and
        cooling/heating degree day information, for the day at a given coordinate location. The data is
        requested for a specified date range, up to 31 days in a single API request. Generally,
        historical data may be available as far back as the last 5 to 40+ years, depending on the
        location.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword start_date: Start date in `ISO
         8601 <https://www.iso.org/iso-8601-date-and-time-format.html>`_ format, for
         example, 2019-10-27. The date range supported is 1 to 31 calendar days, so be
         sure to specify a startDate and endDate that does not exceed a maximum of 31
         days (i.e.: startDate=2012-01-01&endDate=2012-01-31). Required.
        :paramtype start_date: ~datetime.date
        :keyword end_date: End date in `ISO 8601
         <https://www.iso.org/iso-8601-date-and-time-format.html>`_
         format, for example, 2019-10-28. The date range supported is 1 to 31 calendar
         days, so be sure to specify a startDate and endDate that does not exceed a
         maximum of 31 days (i.e.: startDate=2012-01-01&endDate=2012-01-31). Required.
        :paramtype end_date: ~datetime.date
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :return: DailyHistoricalActualsResult. The DailyHistoricalActualsResult is compatible with
         MutableMapping
        :rtype: ~azure.maps.weather.models.DailyHistoricalActualsResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.DailyHistoricalActualsResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_daily_historical_actuals_request(
            format=format,
            coordinates=coordinates,
            start_date=start_date,
            end_date=end_date,
            client_id=client_id,
            unit=unit,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.DailyHistoricalActualsResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_daily_historical_records(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        start_date: datetime.date,
        end_date: datetime.date,
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        **kwargs: Any
    ) -> _models.DailyHistoricalRecordsResult:
        """Use to get climatology data such as past daily record temperatures, precipitation and snowfall
        at a given location.

        The ``Get Daily Historical Records`` API is an HTTP ``GET`` request that returns climatology
        data such as past daily record temperatures, precipitation and snowfall at a given coordinate
        location. Availability of records data will vary by location. Generally, historical data may be
        available as far back as the last 5 to 40+ years, depending on the location.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword start_date: Start date in `ISO
         8601 <https://www.iso.org/iso-8601-date-and-time-format.html>`_ format, for
         example, 2019-10-27. The date range supported is 1 to 31 calendar days, so be
         sure to specify a startDate and endDate that does not exceed a maximum of 31
         days (i.e.: startDate=2012-01-01&endDate=2012-01-31). Required.
        :paramtype start_date: ~datetime.date
        :keyword end_date: End date in `ISO 8601
         <https://www.iso.org/iso-8601-date-and-time-format.html>`_
         format, for example, 2019-10-28. The date range supported is 1 to 31 calendar
         days, so be sure to specify a startDate and endDate that does not exceed a
         maximum of 31 days (i.e.: startDate=2012-01-01&endDate=2012-01-31). Required.
        :paramtype end_date: ~datetime.date
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :return: DailyHistoricalRecordsResult. The DailyHistoricalRecordsResult is compatible with
         MutableMapping
        :rtype: ~azure.maps.weather.models.DailyHistoricalRecordsResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.DailyHistoricalRecordsResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_daily_historical_records_request(
            format=format,
            coordinates=coordinates,
            start_date=start_date,
            end_date=end_date,
            client_id=client_id,
            unit=unit,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.DailyHistoricalRecordsResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore

    @distributed_trace_async
    async def get_daily_historical_normals(
        self,
        format: Union[str, _models.JsonFormat] = "json",
        *,
        coordinates: list[float],
        start_date: datetime.date,
        end_date: datetime.date,
        client_id: Optional[str] = None,
        unit: Optional[Union[str, _models.WeatherDataUnit]] = None,
        **kwargs: Any
    ) -> _models.DailyHistoricalNormalsResult:
        """Use to get climatology data such as past daily normal temperatures, precipitation and
        cooling/heating degree day information for a given location.

        The ``Get Daily Historical Normals`` API is an HTTP ``GET`` request that returns climatology
        data such as past daily normal temperatures, precipitation and cooling/heating degree day
        information for the day at a given coordinate location. The historical normals are a 30-year
        average for temperatures and precipitation for a specific location. As is standard practice in
        climatology, the 30-year average covers years 1991-2020, this data will be used for one decade
        and then will reset in the year 2030. Generally, historical data may be available as far back
        as the last 5 to 40+ years, depending on the location.

        :param format: Desired format of the response. Only ``json`` format is supported. "json"
         Default value is "json".
        :type format: str or ~azure.maps.weather.models.JsonFormat
        :keyword coordinates: The applicable query specified as a comma separated string composed by
         latitude
         followed by longitude e.g. "47.641268,-122.125679".

         Weather information is generally available for locations on land, bodies of
         water surrounded by land, and areas of the ocean that are within approximately
         50 nautical miles of a coastline. Required.
        :paramtype coordinates: list[float]
        :keyword start_date: Start date in `ISO
         8601 <https://www.iso.org/iso-8601-date-and-time-format.html>`_ format, for
         example, 2019-10-27. The date range supported is 1 to 31 calendar days, so be
         sure to specify a startDate and endDate that does not exceed a maximum of 31
         days (i.e.: startDate=2012-01-01&endDate=2012-01-31). Required.
        :paramtype start_date: ~datetime.date
        :keyword end_date: End date in `ISO 8601
         <https://www.iso.org/iso-8601-date-and-time-format.html>`_
         format, for example, 2019-10-28. The date range supported is 1 to 31 calendar
         days, so be sure to specify a startDate and endDate that does not exceed a
         maximum of 31 days (i.e.: startDate=2012-01-01&endDate=2012-01-31). Required.
        :paramtype end_date: ~datetime.date
        :keyword client_id: Indicates the account intended for use with the Microsoft Entra ID security
         model. This unique ID for the Azure Maps account can be obtained from the
         `Azure Maps management plane Account API </rest/api/maps-management/accounts>`_.
         For more information on using Microsoft Entra ID security in Azure Maps, see
         `Manage authentication in Azure
         Maps </azure/azure-maps/how-to-manage-authentication>`_. Default value is None.
        :paramtype client_id: str
        :keyword unit: Specifies to return the data in either metric units or imperial units. Default
         value is metric. Known values are: "imperial" and "metric". Default value is None.
        :paramtype unit: str or ~azure.maps.weather.models.WeatherDataUnit
        :return: DailyHistoricalNormalsResult. The DailyHistoricalNormalsResult is compatible with
         MutableMapping
        :rtype: ~azure.maps.weather.models.DailyHistoricalNormalsResult
        :raises ~azure.core.exceptions.HttpResponseError:
        """
        error_map: MutableMapping = {
            401: ClientAuthenticationError,
            404: ResourceNotFoundError,
            409: ResourceExistsError,
            304: ResourceNotModifiedError,
        }
        error_map.update(kwargs.pop("error_map", {}) or {})

        _headers = kwargs.pop("headers", {}) or {}
        _params = kwargs.pop("params", {}) or {}

        cls: ClsType[_models.DailyHistoricalNormalsResult] = kwargs.pop("cls", None)

        _request = build_maps_weather_get_daily_historical_normals_request(
            format=format,
            coordinates=coordinates,
            start_date=start_date,
            end_date=end_date,
            client_id=client_id,
            unit=unit,
            api_version=self._config.api_version,
            headers=_headers,
            params=_params,
        )
        path_format_arguments = {
            "endpoint": self._serialize.url("self._config.endpoint", self._config.endpoint, "str", skip_quote=True),
        }
        _request.url = self._client.format_url(_request.url, **path_format_arguments)

        _decompress = kwargs.pop("decompress", True)
        _stream = kwargs.pop("stream", False)
        pipeline_response: PipelineResponse = await self._client._pipeline.run(  # type: ignore # pylint: disable=protected-access
            _request, stream=_stream, **kwargs
        )

        response = pipeline_response.http_response

        if response.status_code not in [200]:
            if _stream:
                try:
                    await response.read()  # Load the body in memory and close the socket
                except (StreamConsumedError, StreamClosedError):
                    pass
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = _failsafe_deserialize(
                _models.ErrorResponse,
                response,
            )
            raise HttpResponseError(response=response, model=error)

        if _stream:
            deserialized = response.iter_bytes() if _decompress else response.iter_raw()
        else:
            deserialized = _deserialize(_models.DailyHistoricalNormalsResult, response.json())

        if cls:
            return cls(pipeline_response, deserialized, {})  # type: ignore

        return deserialized  # type: ignore
